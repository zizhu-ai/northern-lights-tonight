# PR #49 F2 / F3 修复说明

日期：2026-09-08。基线：最新 `origin/main`，`998bcb6`（PR #49）。分支：`fix/pr49-f2-f3`。

## 改动

- **F2**：`lib/indexing.ts` 从固定 `https://aurora-tonight.com` 创建 URL，分别设置 pathname/search，再检查最终 origin。避免 `//example.com/a`、反斜杠 host 等输入被当作站外 URL；保留原有主域判断和去尾斜杠行为。
- **F2 回归测试**：补双斜杠、反斜杠、绝对 URL、编码分隔符、userinfo、路径与查询分隔符等输入，检查 origin、凭据、fragment 和查询值。旧实现已实际触发测试失败（origin 为 `https://example.com`），修复后通过。
- **F3**：新增 `lib/indexing-integration.test.ts`，直接执行实际 middleware、搜索 GET 与 next.config.headers；使用真实 NextRequest/NextResponse、真实搜索数据和 Next 自带路径匹配器。测试加载器只负责本地 TypeScript 转译和别名解析，不模拟业务逻辑。
- 覆盖 production 的 308 及主域放行，preview/development/未设置环境时放行；覆盖搜索 slug、坐标、错误和缺省查询的 307/noindex；验证 API（含不存在的 API 路径）匹配 noindex 规则，公开 HTML 路径不匹配。

## 验证

```sh
node --experimental-strip-types --test lib/indexing.test.ts lib/indexing-integration.test.ts
git diff --check
```

结果：8 项测试通过，0 失败、0 跳过；diff 空白检查通过。测试使用 Node v22.22.3；加载器需要 Node 的 `registerHooks`（22.15+）。新测试由现有 `test:unit` 的 `lib/*.test.ts` 自动收集。

额外执行 `npx tsc --noEmit --incremental false`，退出码 2。已用 `git archive origin/main` 提取未修改基线并使用同一依赖执行类型检查，确认两者均只有同样的 3 处既有错误：`acquisition-routes.test.ts:9`、`indexing.test.ts:12` 的 TS5097，以及 `remote-lkg-store.test.ts:508` 的 TS2339。本次未扩大范围修复。

## 边界

仅 F2/F3。未修改 LKG、health、Blob、快照刷新、CSP、安全头配置、robots 策略或 Preview/Vercel 设置。middleware 仍严格以 `VERCEL_ENV === "production"` 启用。未执行部署、合并、完整 Next 服务或线上跳转链检查；API 404 测试证明配置匹配，不能代替线上响应检查。

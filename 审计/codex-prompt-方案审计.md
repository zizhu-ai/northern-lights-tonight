# 任务

你是独立审计方。只读核实下面这套「Northern Lights Tonight」首版方案，不要改文件、不要写代码、不要补做调研搜索。

硬限制（超时 5 分钟，必须把时间留给写报告）：

- 禁止读取任何 skill（包括 spec-cross-audit、cross-cli-audit、~/.agents、~/.mirasim、~/.codex/skills）。本提示词就是全部流程。
- 禁止把 PRD 从头读到尾。PRD 只允许用 rg/grep 抽这几处：URL、SEO、判断引擎、非目标、七天计划、首页个性化相关句。v1 以范围/引擎/页面/线框/档案为准。
- 先读这 4 份短文档（可一次 cat/sed 全文）：`范围｜覆盖地点与产品边界.md`、`判断引擎｜门控规则.md`、`页面｜首版结构.md`、`线框图｜首版.md`。
- `地点档案/wave1.json` 只用 jq 看 meta、15 个 slug 摘要，再抽 colorado / chicago / alaska / seattle / massachusetts 五条。
- 读完上述材料后**立即输出完整报告**，不要再开工具，不要第二轮「全面复核」。

工作目录：

`/Users/zizhu/AGI/1-出海AI产品/产品-northern lights tonight`

必须读完这些文件（可用 Read/Grep；不要读 CSV 全文）：

1. `PRD｜Northern Lights Tonight.md`
2. `范围｜覆盖地点与产品边界.md`
3. `判断引擎｜门控规则.md`
4. `地点档案/wave1.json`（可抽样 2–3 个地点 + 看 meta/字段，不必逐字复述 15 个）
5. `页面｜首版结构.md`
6. `线框图｜首版.md`

不要把 `sem_关键词明细_*.csv` 当主证据；需要词量时以 PRD / 范围 / 档案里的聚类量为准。

用中文写审计报告。只根据上述文档。查不到就写「文档未规定」，禁止脑补实现细节或市场数字。

---

# 产品是什么（供对照，不是让你复述）

美国英语 SEO 工具站：告诉某地用户今晚极光值不值得出门。  
免费 + 以后展示广告。无登录、无 App、无订阅。  
获客是搜索。钱在 `/forecast/[slug]` 地域页；首页是主词枢纽，SSR 不能按 IP 变成某城结论。  
Wave 1 冻结 15 个美国地点。计算可覆盖北半球；俄罗斯不做索引。

---

# 审计问题（逐条回答，不要空过）

## A. 一致性

文档之间有没有互相打架？（PRD vs 范围 vs 引擎 vs 页面 vs 线框 vs 档案）  
特别检查：URL、索引/noindex、首页是否个性化、状态枚举、州页代表点、Alaska 模板、near-me、地图、小时轴位置、SEO 主词承接。

## B. SEO 获客是否成立

按「搜索获客」而不是天气 App 来审：

- 词 → URL 是否一词一页、有无互吃（首页 vs 地点 vs near-me vs where vs 指南）
- 爬虫能否在 HTML 结构里看到答案（不依赖 GPS）
- 15 个地点页是否仍有 doorway 风险（模板换名）
- 主词 `/` 冷启动是否现实；地域页优先是否自洽
- Title/H1/内链/sitemap 有没有明显坑

## C. 判断引擎

门控能否落地、会不会系统性误导用户：

- OVATION 只覆盖约 90 分钟 vs「今晚」窗口
- 中纬远窗禁止 GO 是否合理
- 地平线 8° 采样、云分层、城光、极昼
- UNKNOWN vs NO 的产品后果（SEO 页长期 UNKNOWN 怎么办）
- 档案里的 typical_kp / 偶极磁纬当远窗门槛是否过粗

## D. 范围与地点

- Wave 1 名单、吸收词（Boston、Northern Michigan 等）、Chicago↔Illinois、Alaska↔Fairbanks
- 州页不用几何中心是否在引擎和线框里真正执行
- 「计算宽、索引严」有没有被页面清单破坏

## E. 线框 vs 页面规格

线框有没有漏掉 SEO 强制模块（全量 15 行表、答案段、FAQ、noindex 页）？有没有把不该做的（地图、登录、定位改首页）画回去？

## F. 开工风险

若下一步是「先出 Wave 1 快照、HTML 等线框过了再做」，还缺哪块会让开工卡住或做出错误产品？

---

# 输出格式（必须遵守）

1. **结论**（5 行内）：方案能否按此开工；一句话总评。
2. **严重 / 中等 / 轻微** 三张表。每条必须有：
   - `id`（S1/M1/m1…）
   - 问题
   - 证据（引用文档名 + 关键原句或字段，短引）
   - 若它为真，对 SEO 或用户的影响
   - 建议（可执行，不要空话）
3. **文档打架清单**（没有则写「未发现」）。
4. **建议冻结后再开工的 3 件事**（最多 3 件；不要扩成新 PRD）。
5. **不要做的**（审计认为方案里已经正确禁止、应保持禁止的）。

严重 = 按此做会明显伤排名、误导用户、或文档互相矛盾到无法实现。  
中等 = 不改也能做，但大概率返工或冷启动变差。  
轻微 = 表达不清、缺字段、体验毛边。

不要写「总体很棒」之类开场。不要给百分概率。不要建议做订阅/App/地图，除非指出方案已禁止且应保持。

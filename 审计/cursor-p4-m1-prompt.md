你是核销员。只核 Part 4 的 M1：`TonightPlaces` 是否把过期行打成 UNKNOWN。

只读 `components/tonight-places.tsx` 与 `lib/snapshots.ts` 的 `isSnapshotFresh` / `SnapshotRow`。

若过期行 status 与窗口已降级为 UNKNOWN / —，且分组/排序用降级后的 status：结论必须是「0 严重 0 中等。Part 4 通过，可继续下一部分。」

若未落地，列出剩余中等。禁止改文件。禁止新开轻微清单。中文。

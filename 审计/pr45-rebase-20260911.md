# PR #45 rebase onto main (2026-09-11)

## Result
Rebased `cursor/p0-ux-verdict-consistency-a6db` onto `origin/main` (`03941f7`). Local tip: `ca511e6`.

## Conflicts resolved
1. `app/page.tsx` / `app/forecast/[slug]/page.tsx` / `components/verdict-card.tsx` / `app/globals.css` — kept #45 place ownership + unified verdict presentation; retained main snapshot freshness / sky UI where applicable.
2. `app/api/search/route.ts` — kept main `API_ROBOTS_TAG` / noindex redirects; kept #45 ambiguous/error redirect that preserves `q` onto `/near-me`.
3. Follow-up commit `ca511e6` — adjusted indexing integration test so search input preservation coexists with API noindex after rebase.

## Validation (local)
- `npm test` / unit+engine+parity: passed after test fix
- `npm run build`: passed
- `main` is ancestor of HEAD

## Residual risk
- Needs fresh CI on GitHub after force-with-lease push
- Spot-check Preview for Michigan UNKNOWN consistency + Portland ambiguous search + sticky place bar

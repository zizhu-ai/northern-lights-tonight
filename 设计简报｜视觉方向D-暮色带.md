# 设计简报｜视觉方向 D「暮色带」

**给谁：** grok（执行实现）
**日期：** 2026-08-22
**状态：** 已定稿（spec 交叉审计 round 2：0 严重 0 中等），可执行
**效力：** 本文档取代《设计｜视觉与UI规范.md》第 1–3 节（「白天纸/夜里窗」双层、token、字号表）。该文档的**工程硬规仍然有效**：SSR 结论必须在 HTML、`data-status` 单卡四态换肤、反目标清单、44px 触控、AA 对比。
**探针图：** `视觉稿/probes/lane-d-twilight-band.png`（定稿方向）、`lane-d-v3-no-state.png`（NO 态证明）。图中 Kp 数字、Map/Settings 导航、红色 NO 点均为生成伪影，**不是设计的一部分**。

---

## 1. 一句话方向

把「纸上开一扇夜窗」改成「**半页天空**」：首页上半页是一整块平涂暮色蓝带，问句和搜索在带里；判定卡跨带悬浮，状态词是全页最大、最饱和的元素；带以下是安静的白天纸面。

## 2. 场景句（所有决策的锚）

一个美国北方的普通人，下午在办公室或晚饭后在沙发上，搜 "northern lights tonight"，想知道自己今晚值不值得出门。页面必须在 3 秒内给出答案，并让他相信这个答案不是猜的。

## 3. 锚点参考

- **Dark Sky**：一句话给结论，其余都是解释。克制、自信。
- **yr.no**：公共服务气质。功能、清晰、表格有自己的尊严，无商业感。

## 4. 色彩策略：Committed

暮色蓝带承担 30–50% 的首屏表面。这不是 SaaS 蓝带套路的借口——**材料纪律**决定它是自家货还是套路货：

- 色带**绝对平涂纯色**。禁渐变、禁星星、禁粒子、禁极光照、禁玻璃拟态。
- 极光绿**只**属于 GO 状态和焦点环。不带进装饰。
- 色带的蓝要「暮色」不要「科技蓝」：深、微微偏紫，不荧光。

### Token 表（OKLCH，给出候选 hex，执行时逐个过 AA 再冻结）

| Token | 候选值 | 用途 |
|---|---|---|
| `twilight` | `#1F3875` 附近 | 暮色带。白字其上对比须 ≥ 4.5:1 |
| `night` | `#0A1322` | 判定卡、页脚 |
| `paper` | `#F4F6F8` | 带下页面底 |
| `paper-2` | `#FFFFFF` | 卡片、表格行 |
| `ink` | `#122033` | 正文 |
| `ink-soft` | `#33465C` | 导语、次级（白底 ≥ 4.5:1，替代现 3.37:1 的 muted） |
| `muted` | `#4A5A6C` | 辅助、表头（白底 ≥ 4.5:1） |
| `line` | `#D8DEE6` | 发丝线 |
| `go` | `#3CDBA0` | GO（夜卡上）、焦点环 |
| `maybe` | `#E8B44C` | MAYBE（夜卡上） |
| `no` | `#9AA3B2` | NO（夜卡上；**不用红**） |
| `unknown` | `#7EB0FF` | UNKNOWN（夜卡上） |
| `danger` | `#C2402F` 附近 | 真错误（白底 ≥ 4.5:1，替代现 3.06:1） |

浅底状态胶囊沿用现有四色对，但墨色逐个压到 ≥ 4.5:1（现 UNKNOWN 3.87 / MAYBE 3.48 / NO 4.05 全部不达标）。

## 5. 字体

- **全部 sans。** 不引衬线（用户已拍板原版 D，放弃 Newsreader 方案）。
- UI / 正文 / 状态词 / 表格：Inter（`next/font`，现有 Archivo 移除）。回退栈 `ui-sans-serif, system-ui, sans-serif`。
- **注意：** 本机 dev 曾出现 Google Fonts 拉取失败落到 Times 的情况，执行时验证 `next/font` 产物真实生效（构建日志 + 渲染 DOM 的 computed font）。
- 层级靠字重和字号差拉开，不靠第二字体：

| 角色 | 手机 | 桌面 | 说明 |
|---|---|---|---|
| H1 问句（带内白色） | 34 | 56 | 字重 650–700，`text-wrap: balance`，行高 1.15 |
| 状态词（卡内） | 52 | 64 | **四态同号**。UNKNOWN 不许再缩小（现 `globals.css:522` 的降级必须删掉） |
| 区标题 H2 | 20 | 24 | 现 H2=16px 同正文，必须拉开 |
| 导语 | 18 | 18 | |
| 正文 / UI | 16 / 14 | 16 / 14 | 正文行宽 ≤ 68ch，行高 1.5 |
| Caption / 表头 | 12 | 12 | 不做全大写+宽字距的 eyebrow 套路 |

## 6. 布局策略

### 首页（桌面 / 手机同构）

1. **暮色带**：顶栏融入带内（白字标、白导航、搜索框在带里，输入框用深色底+浅色边，不要白盒子贴在蓝上）。带高以内容为界：H1 + 一句导语 + 搜索，约 40–45vh，不超半屏。
2. **判定卡跨带悬浮**：卡上缘叠在带底缘之上（负 margin 或 transform），这是全页唯一的纵深构图。卡内容顺序不动：状态词 → 一句人话 → 四格 meta（Best window / Main issue / Look / Confidence）→ Updated + Share。
   - **注意**：首页规范原说「不要 IP 结论卡」。新方向下首页卡 = 「你搜索/定位的地点」的卡；未搜索未定位时，卡位渲染**站点级状态**（见第 8 节 P0 修复），不装死。
3. **带下白天纸面**：15 行城市表（桌面表 / 手机整行可点卡）、How to read、FAQ、指南链接、夜色页脚收口（页脚用 `night` 色，与带呼应）。

### 钱页（forecast/[slug]）

暮色带的**瘦版**：带内只放 kicker（州 + 时区）+ H1 + 答案段，判定卡同样跨带悬浮。首屏手机不滚必见状态词的硬规不变。右栏 Nearby（桌面粘性）这次一并补上——现在 680px 栏漂在 840px 页里，右侧空白像半成品。

### near-me / guides / methodology / 404

- near-me：工具页，浅底，可只用细暮色顶条（3–4px）作身份呼应，不必整带。
- guides / methodology：长文，白底，`ink` 正文，衬线已弃用，靠 H2 层级和行宽 68ch 保证阅读感。
- 404：浅底人话 + 吸收目标，不用夜卡。

## 7. 判定卡细节

- 圆角 16（现 6，改掉）。阴影：唯一允许的大阴影 `0 12px 40px rgba(10,19,34,.30)`。
- 原规范的「上沿 3px 状态色条」**取消**——跨带悬浮构图本身承担状态表达，加色条是双重语气。
- 状态 = 词 + 色 + 人话，颜色永不单独表意。NO 用冷灰不用红。红只给 GPS 失败 / 500。
- Share 是卡内次按钮，不盖过状态词。

## 8. 必须一并修掉的评审 backlog（P0–P2，全部在范围內）

1. **P0 站点级 stale 表达**：快照全部 UNKNOWN/过期时，首页判定卡位渲染一条诚实状态——"Readings paused: source data is stale. We don't guess."——而不是 15 行相同的 UNKNOWN 让用户逐行发现。
2. **P0 时间戳人性化**：<60min → "N minutes ago"；<24h → "N hours ago"；否则绝对时间 "Updated Tue 9:14 PM ET"。消灭 "1679 minutes ago"。
3. **P1 对比度**：按第 4 节 token 表修 `--muted` / 胶囊墨色 / `--danger`，逐个过 AA（正文 ≥ 4.5:1）。
4. **P1 字体层级**：按第 5 节执行（H2 拉开、四态状态词同号、H1 上 34/56）。
5. **P2 首页行热区**：整行可点、最小高 44px（现只有 ~20px 地名链接）。顺手修孤 "—" 行和 "Chicago / Chicago" 重复行。
6. **P2 /view 空指针岛**：无有效坐标时渲染 find-place 空态，禁止 "0.000, 0.000"。
7. **P2 上线前必摘**：`layout.tsx` 的 `robots: noindex` 和 "Pipeline stub" 描述（上线时再摘，现在标记 TODO 即可）。
8. **杂项**："Tonight's hours" 直撇号统一为 typographic 撇号；卡片 meta 里 "Look north / North" 同义反复改成单一信息量（值或标签留一个）。

## 9. 交互与动效

- 动效 ≤ 200ms，ease-out，`prefers-reduced-motion: reduce` 全关。
- 判定卡不做入场编排动画；页面加载即任务就绪。
- Find place 维持现有工程（手机底栏 sheet / 桌面弹层），只换皮肤；补一个**可见关闭按钮**（现在只能点背板）。
- 焦点环用 `go` 绿；暮色带内焦点环用白色，保证非文本 3:1。

## 10. 反目标（沿用，一条不放松）

无库存极光/星空图、无粒子、无 WebGL、无 Kp 仪表、无百分环、无地图、无渐变文字、无玻璃拟态、无全大写 eyebrow、无 01/02/03 编号段标、无暗色整套互斥布局。NO 不用红。结论 SSR 直出，关 CSS 仍可读。

## 11. 验收

- 手机 390px 打开任意钱页：不滚动见状态词 + 人话。
- 首页首屏：带、卡、表的层级一眼成立；关掉所有图片/字体回退依然成立。
- 对比度脚本或手工抽查：第 4 节所有标注 ≥ 4.5:1 的组合全部达标。
- 全 UNKNOWN 状态下首页读得出「诚实」而不是「死了」。
- `/view` 裸访问不再是空指针岛。
- 重跑 `/impeccable critique` 并归档报告到 `.impeccable/critique/`；本条只要求跑通归档，不设分数闸。

## 12. 明确不做（本轮）

- 不做暗色模式整套（暮色带是构图，不是主题切换）。
- 不动 URL、模块顺序、GO/MAYBE/NO/UNKNOWN 判断逻辑、引擎代码。
- 不加 recents / 账号 / 广告位。

---

## 执行备注（审计轻微项，供执行者参考，不构成验收闸）

1. **效力范围：** 本文只废止旧《设计｜视觉与UI规范.md》第 1–3 节；其工程硬规（SSR、`data-status`、反目标、44px、AA）继续有效。首页判定卡按本文 §6 执行，不再受旧文「不要 IP 结论卡」约束。
2. **「不动模块顺序」** 指 FAQ/指南/小时轴等既有模块不重排；首页新增判定卡位是本文 §6 的正面新增，不与之冲突。
3. **`twilight` 色值**「`#1F3875` 附近」是候选起点而非唯一值；验收只卡白字其上 ≥ 4.5:1，不卡具体 hex。
4. **带内导语用白色**（H1/顶栏已写明白色；`ink-soft` 的「导语」用途限定在白底，不要铺到暮色带上）。
5. **首页卡数据源：** 未搜索未定位且非全 stale 时，复用 15 行表已有的四态数据渲染卡位（默认代表点），不新造全国聚合、不用 IP 定位；P0 的人话只替换全 UNKNOWN/过期场景。

### 实现审计轻微项（第 1 期，0S/0M 定稿后记录，2026-08-22）

1. `app/forecast/[slug]/page.tsx:118` — 钱页带内 kicker 多了一段 "Headline: {point}"（§6 写的只放州+时区）。
2. `app/part4.module.css:265`、`app/forecast/[slug]/page.module.css:294` — 桌面带 `min-height: calc(42vh - header)` + padding，瘦版带未做，视觉带可能略超半屏（即复评 P3）。
3. `app/part4.module.css:53-59,269-272` — `.hero h1` 的 34/56 铺到了指南/methodology/near-me/404；§5 该档定位是「带内白色 H1」。
4. `components/tonight-places.module.css:98` — 手机卡化断点 599px，与全站 768 不一致。
5. `app/globals.css:295` — Find place 面板仍有 `0 12px 32px` 阴影；§7 说判定卡是唯一大阴影（遮罩层，不挡验收）。

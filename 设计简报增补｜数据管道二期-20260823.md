# 设计简报增补｜数据管道二期：请求驱动重算 + 按源 freshness（2026-08-23）

> **已被取代：** 本文件仅保留为历史背景。当前上线加固的唯一批准规范是 [`docs/superpowers/specs/2026-08-24-launch-hardening.md`](docs/superpowers/specs/2026-08-24-launch-hardening.md)；冲突时以该规范为准。

**地位：** 工程实施简报。不改任何视觉、文案、路由结构；视觉仍以《设计简报｜视觉方向D-暮色带.md》+《设计简报增补｜清账-20260822.md》为准。  
**依据：**《审计/codex-pipeline-options-20260823.md》§四–§七（codex 独立架构复核）。一期已上线（PR #8）：ISR 读 Git `main` 快照，见《设计简报增补｜数据管道一期-20260823.md》。  
**本轮范围：** 只定二期实施合同。简报未经审计不得写产品代码。

一期之后仍成立、且必须由二期拆掉的结构问题：

- GitHub Actions 仍是唯一数据生产者；调度延迟即可让站点失去活性。
- `valid_until = generated_at + 25min` 一刀切，把传输 TTL 当成业务证据。
- engine 抓数失败仍正常退出（workflow `success` ≠ 数据健康）；OVATION 与 Kp 同一 `try`，OVATION 失败则 Kp 根本不拉。
- 每天约 144 次 commit + deploy，用发布单元运输约 248 KB JSON。
- `engine/snapshot.py`（约 800 行）与页面 TS 之间无运行时合同校验；已观测漂移：Python 输出 `cloud_block: "socked"`，TS 声明 `"blocked" | "unknown"`。

---

## 1. 目标与非目标

### 目标

- **站点活性不再依赖「GitHub 调度 → Python 抓数 → commit → Vercel 构建 → 部署」全链。** ISR 页面 render 时经全站单一入口 `getAuroraBundle()` 拉原始上游，用**当前 `now`** 重算 15 个地点的派生 snapshot。
- **缓存原始 source envelopes，不缓存「当时 now」冻住的派生 verdict 当长期真理。** 成功则把通过校验的 raw envelopes 持久化为 last-known-good；失败则在各源硬限内降级，而不是立刻全站 UNKNOWN。
- **按源分级 freshness**：废掉 25 分钟一刀切。UNKNOWN 恢复为「没有仍适用于当前决策窗口的 aurora 证据」，而不是「任务 25 分钟没跑」。
- **单一计算核心：** 用固定 raw fixtures 锁定 Python 当前输出，将纯计算迁到 `lib/aurora-engine/` TypeScript，逐 fixture 对齐后切流，退役 Python producer。禁止长期两套分类规则。
- **观测可机械读取：** `/api/snapshots/latest` 暴露 `generated_at` 与每源 `source_time` / `fetched_at` / `age_seconds` / `health` / `fallback_used`。
- **合同不再靠 type assertion：** 外部输入与持久化 envelopes 做版本化运行时校验；`cloud_block` 以引擎实际输出 `"socked"` 为准，修正 TS 类型，不把 `"socked"` 偷偷改成 `"unknown"`。

### 非目标

- **不改视觉 / 文案 / 路由结构。** 不改 CSS、不改 `REASON_COPY` 固定句、不改 `/forecast/[slug]` 与 guides 信息架构。degraded 时「显示真实 source age」只复用现有 `formatUpdatedAt`（输入改为真实 source/fetched 时间），不新增组件、段落或文案键；若审计认为这仍算文案变更，见 §9。
- **不动 noindex。** `app/layout.tsx`、`app/page.tsx`、`app/forecast/[slug]/page.tsx`、`app/robots.ts`、bundle `seo_indexable: false` 一律保持。收录是上线 gate，另议，禁止与本管线混在一个 patch。
- **不引入新调度器依赖。** 不以 Vercel Cron、新的腾讯云 cron、或任何「第二个心跳」作为数据正确性边界。报告已写明：Hobby Cron 最高每日一次；Cron 是调度器，不是 freshness 真理。一期为 workflow 加的腾讯云 `workflow_dispatch` 随 Python producer 一起退役，不改派为二期生产者。
- 不把「只把 `valid_until` 改成 2 小时 / 跟 NOAA issue time」当终态（报告 §三否决）。
- 不把 39 路 Open-Meteo 串行、timeout 45s 的 Python 抓法原样搬进 Vercel Function。
- 不新增除 `@vercel/blob` 以外的 npm 依赖（含测试框架、schema 库、队列库）。校验用手写 type guard；测试用 Python stdlib + Node 内置 `node:test`。
- 不在本 patch 做 Open-Meteo 商业条款签约、Vercel 套餐升级、独立 watchdog 调度器。这些进 §9。
- 不改 Wave 1 地点集合、档案、`/view` 无档案路径的产品范围。

---

## 2. 数据流设计

### 2.1 目标数据流（与报告 §四一致）

```text
request / bot
    ↓
Next ISR page (force-static, revalidate = 600) 或 /api/snapshots/latest (revalidate = 600)
    ↓ server-side only，禁止客户端补丁 verdict
getAuroraBundle()
    ├─ getSourceEnvelopes()     ← 全站唯一 10 分钟缓存 key（raw，不是派生 verdict）
    │     ├─ 独立刷新 OVATION / Kp / Open-Meteo（校验 + fetched_at + fingerprint + coverage）
    │     ├─ 成功：写入 Vercel Blob last-known-good raw envelopes
    │     └─ 失败：读 Blob LKG；Blob 也不可用时才读部署内 bundled snapshots/
    │           各源必须落在自己的 hard limit 内，否则该源作废
    └─ 用调用时刻的 now 重算全部 15 个地点（同一份 envelopes，同一 now）
    ↓
Home / forecast metadata / forecast HTML / JSON-LD / latest API 使用同一 bundle
```

关键约束：

- **全站一个 raw 缓存 key**，禁止 15 个 forecast 页各自打 NOAA。OVATION payload ~900 KB，只拉一次。
- 派生 snapshot **每次**用当前 `now` 重算：`night_slots` / `rollup` / `best_window` / 顶层 status / confidence / `answer_sentence` 都依赖 now。缓存「当时算好的 GO」会把已结束的窗口当成现在的答案（报告 §三）。
- ISR 600s 只决定 HTML 再生频率；再生时 `now` 必须前进。
- Server Component 必须 `await` 这份 bundle。verdict 继续在首屏 HTML 里。guides / methodology 仍不走 verdict 路径（与一期事实一致）。

### 2.2 模块职责

| 模块 | 职责 |
|---|---|
| `lib/aurora-sources.ts` | 集中抓取；timeout；有界 retry + jitter；User-Agent；conditional GET（`ETag` / `Last-Modified`）；内容 fingerprint；schema validation；写入每源 `fetched_at` 与 coverage。OVATION 与 Kp **独立**请求、独立失败、独立缓存。Open-Meteo **multi-coordinate / batch**（或有限并发），禁止 39×45s 串行。 |
| `lib/aurora-engine/` | 纯计算。输入：`now` + 已校验 envelopes + Wave 1 档案。输出：15 地点 snapshot + `latest` bundle。无 IO。 |
| `lib/live-snapshots.ts` | `getAuroraBundle()` / `getSourceEnvelopes()`。拼缓存、single-flight、Blob LKG、bundled fallback、source health。 |
| `lib/snapshots.ts` | 现有调用方的稳定门面：`loadLatest()` / `loadLatestWithMeta()` / `loadForecastSnapshot(slug)` 改为读同一 bundle。`isSnapshotFresh` 不再看 `generated_at+25min`。 |

### 2.3 Single-flight / 防击穿

三层，都不引入新调度器、不引入分布式锁库：

1. **平台 Data Cache（跨 isolate）：** `getSourceEnvelopes` 用 Next `unstable_cache`（key 固定为 `aurora-source-envelopes-v1`，`revalidate: 600`）。15 个页 + API 共享同一 raw key，命中不打上游。
2. **请求内去重：** `getAuroraBundle` 用 React `cache()` 包一层。首页当前会并行 `loadLatest()` + `loadTonightRows()` → 两次 `loadLatest`；同一 render 只算一次 bundle。
3. **isolate 内 in-flight Promise：** 模块级 `inflight: Promise<Envelopes> | null`。cache miss 撞在一起时，第二个等待第一个的 fetch，禁止并行打满 NOAA / Open-Meteo。

击穿时的正确性靠 Blob LKG，不靠「一定会打到上游」：

- fetch 超时 / 5xx / 校验失败 → **不写 Blob**（禁止错误数据污染 LKG）。
- 返回 Blob 上一份已校验 envelopes，标 `fallback_used=true`，按 §4 硬限裁剪。
- Blob 读失败且 envelopes 缓存未命中 → bundled `snapshots/*.json` **仅**用于 build / 冷启动 / 外部持久层暂时不可用。bundled 是派生快照、不能用当前 now 重算剩余夜；必须当 degraded，且仍受硬限约束。**禁止**因为「来自 Git」就永久 live。
- 不把 Vercel Cron 当补偿心跳。

refresh target（10 分钟）到期本身只触发下一次尝试，不触发 UNKNOWN。

### 2.4 持久化合同（Blob）

Blob 存的是 **raw source envelopes**（OVATION 原文、Kp 原文、Open-Meteo 原文或按点 map），外加：

```text
schema_version
ovation: { fetched_at, source_time (Observation Time), fingerprint, etag?, payload }
kp:      { fetched_at, fingerprint, etag?, time_tag coverage, payload }
cloud:   { fetched_at, fingerprint, model/coverage, payload }
```

不在 Blob 里存派生 GO/MAYBE 当 LKG 真理。派生只存在内存 bundle 与 ISR HTML。

允许新增依赖仅 `@vercel/blob`。凭证走 Vercel 环境变量（`BLOB_READ_WRITE_TOKEN`），不进仓库。

KV 是否改用：见 §9，本简报不在 KV 与 Blob 之间另开一条实施路径；代码按 Blob 写，审计可驳回。

### 2.5 页面与 API 的时间语义

报告要求区分三套时间，禁止再把一次计算时间包装成上游新鲜度：

| 字段 | 含义 |
|---|---|
| `generated_at` | 本次 bundle **重算**时刻（`now`）。API 继续用它证明「数据在前进」。 |
| 每源 `source_time` / `fetched_at` / `age_seconds` | 上游产品时间 vs 我们真正拿到的时间。 |
| UI `Updated` | 健康时可以继续用 `generated_at`；**degraded 时必须改用该地点仍在用的源的真实 age**（经现有 `formatUpdatedAt`），不能显示「刚更新」而实际在吃 80 分钟前的 OVATION。 |

`valid_until`：停止作为页面门闩。二期计算路径不再写 `generated_at + 25min` 作为 freshness 真理。为减少调用方崩裂，JSON 里可暂留该键，但语义不得再被 `isSnapshotFresh` 使用。

---

## 3. Python → TS 迁移计划

报告：「不要长期维护两套分类规则。」顺序锁死如下。任一步 parity 失败不得切流。

### 3.1 拆 Python：fetch vs pure compute

先改 `engine/snapshot.py`（可拆文件，但对外 CLI 保持 `python3 engine/snapshot.py`）：

- **fetch：** OVATION、Kp、Open-Meteo。本步起就必须 **独立 try**（修复「OVATION 失败则 Kp 不拉」）。fetch 失败记录错误，不 `sys.exit(0)` 伪装健康；CLI 退出码：全部地点写出但仍有源失败 → 非零（避免再拿 workflow success 当 NOAA 健康）。在 Python producer 退役前，workflow 对非零退出的具体处理见 §8，不在本步偷偷改调度语义。
- **pure compute：** 函数签名等价于  
  `compute_bundle(now, ovation_envelope, kp_envelope, cloud_envelopes, dossiers) -> bundle`  
  不读网、不写文件、不读 `engine/.cache`。`night_slots` / `classify_window` / `rollup` / `snapshot_location` 全部只吃 `now` + envelopes。
- CLI `main()`：fetch → compute → 写 `snapshots/`。`--offline` 仍读 cache，仅供 fixture 再生。

在改 TS 之前，Python compute 的输出必须与拆分前 **逐字段一致**（用下面 goldens 锁）。

### 3.2 Golden fixtures（锁 Python **当前**输出，不是「理想」输出）

目录建议：`engine/fixtures/<case>/`

每个 case 固定：

- `now`（ISO UTC）
- raw OVATION JSON
- raw Kp JSON
- raw Open-Meteo（按 sample point key，15 地点共 39 点）
- `expected/latest.json` + `expected/<slug>.json`（由**拆分后、切流前**的 Python compute 生成，一经录入不得手改）

跑法：Python `unittest` 调 `compute_bundle`；TS 侧 Node `node:test` 读同一 fixtures，调 `lib/aurora-engine`。**不新增** jest/vitest/tsx 依赖。TS 测试用 Node 当前已支持的 type-stripping，或由现有 `tsc` 能编译的测试入口执行——实施时选零新依赖的一种，禁止为测试加包。

### 3.3 Fixture 必须覆盖的场景

最低集合（少一条不得宣称 parity 完成）：

| case id | 锁什么 |
|---|---|
| `happy_fresh` | 三源齐全、OVATION obs 新、今晚 slots 完整；15 地点基线。 |
| `cross_midnight` | 本地夜跨日。`now` 在地点时区 23:40 附近，剩余窗必须含 00:00 之后，且已结束的日落前窗不得进入 rollup。至少覆盖一个中纬州（`America/Chicago` 或 `America/New_York`）和一个 Alaska 时区。 |
| `dst_spring_forward` | 美国春拨。`now` 落在 2026-03-08 前后、`America/New_York`（Massachusetts）与 `America/Indiana/Indianapolis`（Indiana 档案时区，报告已点名该特殊性）。窗口 ISO 与本地 hour label 不得出现不存在的 02:30。 |
| `dst_fall_back` | 秋拨重复小时。`now` 落在 2026-11-01 前后同一时区；`night_slots` 不丢不重到导致 `best_window` 错位。 |
| `best_window_elapsed` | 同一份 raw，把 `now` 推到所有原 GO 窗 `end` 之后。rollup 只看未结束窗；顶层 status / `best_window_*` 必须变；禁止把已过 GO 继续当今晚答案。 |
| `ovation_missing` | OVATION 缺或校验失败。近窗 `DATA_MISSING_AURORA` / UNKNOWN；远窗仍可走 Kp（与现引擎一致）。Kp 必须仍然被 fetch（独立失败语义）。 |
| `kp_missing` | Kp 缺。近窗仍可用 OVATION；远窗无 Kp → UNKNOWN / `DATA_MISSING_AURORA`（有档案）。 |
| `aurora_both_missing` | OVATION+Kp 都缺 → 地点不得猜出 GO。 |
| `cloud_partial_missing` | 39 点中部分 Open-Meteo 失败。失败点按现引擎：aurora `none` 仍可 NO + `DATA_MISSING_WEATHER`；否则 MAYBE / low，禁止 GO。 |
| `cloud_all_missing` | 同上，全点无云。 |
| `ovation_age_46m` | Observation Time 相对 `now` >45min 且 ≤90min：近窗仍可用，confidence 上限 medium（现 `ovation_stale_45`）。 |
| `ovation_age_91m` | >90min：近窗不得用 OVATION 驱动 verdict（现 `ovation_stale_90` / `ovation_ok=false`）。 |
| `kp_slots_missing` | JSON 有体，但今晚所需 3 小时 `time_tag` slots 缺（coverage 失败）。按 §4 Kp 硬限处理，不得假设「文件还在就有效」。 |
| `malformed_ovation` | 缺 `coordinates` / 无法解析 Observation Time。不得写入 LKG；不得当 live。 |

`happy_fresh` 与 `best_window_elapsed` 必须是同一份 raw、两份 `now`，用来证明「缓存 raw + 重算 now」而不是「放宽 TTL」。

Polar / `NEVER_DARK`（Alaska 盛夏 `night_slots` 为空）不是用户点名的最低集，但 `night_slots` 跨 48h 逻辑与跨日相邻；有现成档案则加 `alaska_never_dark`，没有则在 §9 标明未锁。

### 3.4 迁 TS、对齐、切流、退役

1. 把 astro（太阳高度 / 月亮）、`ovation_at` / `ovation_reach`、`kp_at` / `kp_reach`、`cloud_block_at`（继续输出 `"socked"`）、`classify_window`、`rollup`、`apply_midlat_confidence`、`night_slots`、`snapshot_point` / `snapshot_location`、`_answer` 迁到 `lib/aurora-engine/`。行为以 Python goldens 为准，不「顺手修好」门控。
2. 逐 fixture 对齐。通过标准见 §7 parity（15 地点全字段）。
3. **先切读路径、后停写路径：** runtime `getAuroraBundle()` 改走 TS compute；Python workflow 仍可写 `snapshots/` 当 bundled 冷启动，直到生产核心验收通过。
4. 生产验收通过后再退役 Python producer（workflow 不再 commit，见 §8）。`engine/snapshot.py` 可留作 fixture 再生 oracle，不再担任网站 liveness。

禁止：页面里再写一套简化分类；TS 未对齐就删 Python；把 `"socked"` 改成 `"unknown"` 来迁就过时类型。

---

## 4. 按源分级 freshness

废掉页面层 `isSnapshotFresh(valid_until)` 的 25 分钟全局否决。freshness 在 envelopes 上算，再喂给现有门控。

### 4.1 OVATION

沿用现引擎，不以传输 TTL 覆盖：

- 时间锚点：产品 `Observation Time`，不是我们的 `generated_at`。
- `now - Observation Time > 45min` → 降 confidence（high→medium），近窗仍可用。
- `now - Observation Time > 90min` → 近窗不得用 OVATION 驱动 verdict（`ovation_ok = false` → `DATA_MISSING_AURORA` / UNKNOWN）。远窗仍可走 Kp。
- 缺失 / 无法解析 / 超 90min：该源 hard invalid，不得用过期网格假装 live oval。

### 4.2 Kp

报告原话：JSON **没有**可直接用的整体 issuance field；禁止假设「每 10 分钟必变」。

必须记录：

- 我们的 `fetched_at`
- payload fingerprint（稳定 hash）
- **今晚决策窗口需要的 3 小时 `time_tag` slots 是否都存在**（coverage）

判定：

- 今晚所需 slot 缺失 → 该窗 Kp 不可用，走现引擎「远窗有档案但无 Kp → UNKNOWN / `DATA_MISSING_AURORA`」。
- 软/硬年龄的具体分钟数：**本简报不发明数字。** 实施前用连续观测校准（开放问题 §9）。在数字定稿前，**coverage 失败 = 硬无效**；仅 `fetched_at` 变老但 slots 仍覆盖今晚时，不得单凭「我们有一会儿没拉到新 JSON」把已覆盖的预报整份作废。

### 4.3 Cloud（Open-Meteo）

这是对**今晚**的预报，不是 live oval。

- **不按 25 分钟作废。**
- 记录 `fetched_at`、模型/时间覆盖（hourly `time[]` 是否盖住今晚剩余 mids）。
- 覆盖住今晚剩余窗 → 可用，哪怕 `fetched_at` 已超过 25 分钟。
- 超硬限（覆盖不够，或 `fetched_at` 超过将校准的硬限——数字见 §9，简报不编）：按现引擎 `DATA_MISSING_WEATHER`：
  - aurora **明确到不了**（`aurora_reach = none`）→ 仍可 **NO** + `AURORA_NO_REACH`（可带 `DATA_MISSING_WEATHER`）。
  - 否则 → 封顶 **MAYBE / low**，禁止 GO。
- 不因缺云把整站打成 UNKNOWN。

### 4.4 源健康如何替换 `isSnapshotFresh`

`lib/snapshots.ts`：

- `loadLatest` / `loadForecastSnapshot` 返回的地点 `status` **已经是**用当前 `now` + 源健康算过的结果。
- 页面去掉「`valid_until` 过期则强制 UNKNOWN + `DATA_STALE`」这条运输层否决。
- **`isSnapshotFresh` 不得再改任何卡片的 status**：删除其在 `getForecastState` / `HomeVerdict` / `displayTonightStatus` 里作为门闩的用法（函数可保留或删除，但不得再影响展示状态）。`displayTonightStatus` 只返回引擎 `status`。
- **`fallback_used=true` 或 `health≠ok` 本身不得把卡片打成 UNKNOWN**：LKG 在硬限内时，卡片显示的就是重算后的 GO/MAYBE/NO，`data-status` 与该状态一致。degraded 只改 `formatUpdatedAt` 的时间输入（§2.5）和 API 头/JSON，不改 `data-status`、不改卡片结构。
- `live` / `stale` 门闩删除；全站 paused（`isSiteReadingsPaused`）仅当 15 个地点的 **引擎 status** 都是 UNKNOWN。
- `DATA_STALE` 文案键保留，但二期正常路径不再用 25 分钟 TTL 把它打到卡片上。
- 旧 GO 不得只因为「LKG 里还写着 GO」而显示；必须经当前 `now` 重算且源未 hard invalid。

---

## 5. UNKNOWN 新语义

`UNKNOWN` = **无法用仍然适用于当前决策窗口的 aurora 证据得出可负责的结论。** 不是「GitHub 没跑 / Blob 没写 / 距 generated_at 25 分钟」。

三层（报告 §四）：

### 5.1 refresh target（软，10 分钟）

- envelopes 缓存 10 分钟到期 → 下次 ISR/API 尝试抓新数据。
- **本身不触发 UNKNOWN**，也不改 status。
- 抓新成功：更新 Blob LKG，用新 envelopes + 当前 now 重算。
- 抓新失败：进 5.2，不进 5.3（除非已经破硬限）。

### 5.2 degraded（LKG 窗口）

- 上游失败，但该源仍在硬限内 → 用 last-known-good。
- **降 confidence**（只砍不升；已有 `DATA_MISSING_*` 路径本来就会 low）。
- **显示真实 source age**（§2.5），并在 API 标 `fallback_used=true`、`health=degraded`。
- 用当前 `now` 重算：已过的 `best_window` 必须掉出 rollup。这不是 UNKNOWN，是过期窗口的正常消失。
- 前 10 分钟运输失败 **不得**立刻全站 UNKNOWN（§7 断源演练）。

### 5.3 hard invalidity（按源硬限）

- OVATION：§4.1 的 45/90。
- Kp：今晚所需 3h slots 不存在，或（校准后的）硬年龄——§4.2 / §9。
- Cloud：§4.3；硬限后是 `DATA_MISSING_WEATHER` 封顶，不是自动 UNKNOWN。

aurora 数据缺失或超过硬限：保留现有 `DATA_MISSING_AURORA` / `UNKNOWN`。天气再完整也不能冒充极光。

### 5.4 安全边界：旧 GO 不得越过

下列任一成立，禁止继续展示无标识的旧 GO：

- 支撑该 GO 的近窗 OVATION 已 >90min 且远窗也不能按门控给出 GO。
- 支撑该 GO 的窗口 `end <= now`（已过的 best window）。
- aurora 两源均 hard invalid。
- LKG / bundled 无法通过 schema 校验。

允许：aurora 明确 `none` 时的 NO（高置信「今晚到不了」仍是可负责结论）。  
禁止：为了「不出 UNKNOWN」而无限期服务旧 GO。

---

## 6. 改动文件清单

允许新增 npm 依赖 **仅** `@vercel/blob`（`package.json` / `package-lock.json`）。其余零新增。

### 新增

| 路径 | 做什么 |
|---|---|
| `lib/aurora-engine/` | 纯计算（astro、ovation/kp/cloud 解释、classify、rollup、night_slots、serialize）。无 IO。 |
| `lib/aurora-sources.ts` | 独立 fetch、timeout/retry/jitter、conditional GET、fingerprint、运行时 schema、Open-Meteo batch。 |
| `lib/live-snapshots.ts` | `getSourceEnvelopes` / `getAuroraBundle`；`unstable_cache` + `cache()` + inflight Promise；Blob LKG 读写。 |
| `engine/fixtures/**` | §3.3 各 case 的 raw + expected。 |
| `engine/test_compute.py` | Python goldens（stdlib `unittest`）。 |
| `lib/aurora-engine/*.test.ts` 或 `scripts/aurora-engine-parity.mjs` | 同一 fixtures 的 TS parity（`node:test`，零新依赖）。 |

### 修改

| 路径 | 做什么 |
|---|---|
| `engine/snapshot.py` | 拆 fetch / compute；独立拉 OVATION 与 Kp；compute 不写文件；CLI 仍可写 snapshots 直到退役。抓数失败非零退出（与「success ≠ 健康」对齐）。 |
| `engine/README.md` | 反映 fetch/compute 与「不再是站点 liveness」。 |
| `lib/snapshots.ts` | 门面改读 `getAuroraBundle()`；`cloud_block` 类型改为引擎真值 `"clear" \| "mixed" \| "socked"`（可保留 `null`）；`isSnapshotFresh` 按 §4.4 不得再影响卡片 status；`loadLatestWithMeta` 的 `source` 取值锁死字面 `live | lkg | bundled`（观测用）。保留 `formatUpdatedAt` / `formatWindow`。 |
| `app/page.tsx` | 保持 `force-static` + `revalidate = 600`。`HomeVerdict` 继续 await 统一 bundle。去掉 25min 强制 UNKNOWN。metadata / robots **不动**。Updated 在 degraded 时走真实 source age。 |
| `app/forecast/[slug]/page.tsx` | 同上。`getForecastState`、`generateMetadata`、JSON-LD 与 HTML 同一 bundle。robots / noindex **不动**。`displayEnum("socked")` 可继续显示 “Socked”；不改布局。 |
| `components/tonight-places.tsx` | `displayTonightStatus` / `isSiteReadingsPaused` 不再用 25min TTL；paused 仅当 15 行引擎 status 皆 UNKNOWN。 |
| `app/api/snapshots/latest/route.ts` | 读同一 runtime bundle；`revalidate = 600`（一期是 120，服务 Git raw；二期对齐 ISR）。去掉对「无再验证 build artifact」的依赖。响应头与 JSON 见 §7 观测入口。保留 `X-Robots-Tag: noindex, nofollow`。 |
| `.github/workflows/refresh-aurora-snapshots.yml` | **切流前可暂留**（bundled 冷启动）。生产核心验收通过后：停止每 10 分钟 commit/deploy。允许留 `workflow_dispatch` 的手动 / 低频 **不发布** 检查，但不得再 `git push` 到 `main` 当 liveness。禁止加 Vercel Cron 顶替。 |
| `package.json` / `package-lock.json` | 只加 `@vercel/blob`。可加 `npm` script 跑 goldens，脚本本身零新依赖。 |

### 明确不改

- `app/layout.tsx` robots、`app/robots.ts`、各页 `seo_indexable` / `index: false`
- `content/ui-copy.json`、guides markdown、CSS modules、`components/verdict-card.tsx` 结构
- `地点档案/wave1.json`、`data/us-places.json`、`lib/forecast-places.ts`（读档除外）
- `app/view/page.tsx`、guides、methodology、near-me 路由
- 一期 `SNAPSHOT_REMOTE_BASE` 可留作 bundled/Git 故障注入，但不再是新鲜度主路径

`snapshots/*.json`：切流后不再被 workflow 刷新；保留在仓库只作冷启动 fallback，且受硬限约束。

---

## 7. 验收标准

全部可机械判定。写法吸取一期教训：**只写单侧不等式，不写「之差」**；观测入口写死为 API 头或 JSON 字段。因 ISR stale-while-revalidate，跨过 10 分钟的 **第一次** 响应允许仍是旧 HTML；判定用紧接着的第二次请求或 `x-vercel-cache` 不是 HIT 的那次，禁止把第一次 stale 当失败。

本地验证必须 `next build && next start`，禁止只在 `next dev` 签字。

### 7.1 构建与页面未崩

1. `npm run build` 退出码 0。
2. `next start` 后：`GET /`、`GET /forecast/chicago`、`GET /forecast/alaska`、`GET /forecast/fairbanks` 均为 200；`GET /forecast/boston` 仍为 404（静态 `notFound()`，不经 bundle）。
3. `curl` 首页与上述 forecast **原始 HTML**（非 hydration 后 DOM）含 `data-status=`，以及该页 verdict 的 status / best window 文本。证明仍是 Server Component HTML，不是客户端补丁。

### 7.2 Parity（切流门闩）

对 §3.3 **每一个** fixture，Python `compute_bundle` 与 TS `lib/aurora-engine` 在 15 个地点上必须逐字段对齐：

- `status`
- `confidence`
- `reason_codes`（有序；与 Python 现实现一致，不是集合近似）
- `best_window_start` / `best_window_end`（毫秒级相同；皆 `null` 也算对齐）
- 报告要求的补充：`points[]` 每点 `id/status/confidence/aurora_reach/cloud_block/main_obstacle`；headline `windows[]` 的 `start/end/skip/status/aurora_reach/cloud_block/source/codes`

`cloud_block` 真值是 `"socked"` / `"mixed"` / `"clear"` / `null`，禁止 TS 把 `"socked"` 写成 `"unknown"` 或 `"blocked"` 后声称对齐。

少一个地点、少一个 case、或只比对首页代表行（Fairbanks），本条失败。

### 7.3 Fake clock（与 parity 共用 fixtures）

用固定 `now`，不依赖墙钟：

- `cross_midnight`：rollup 所用窗的 `end` 均晚于该 case 的 `now`；存在 `start` 日期与 `end` 日期不同的窗（跨日）。
- DST 两案：输出窗口 ISO 经该时区转换后，不出现该日不存在的本地钟点。
- `best_window_elapsed`：相对 `happy_fresh` 同一 raw，`best_window_end` 为 `null` 或 **不晚于** 新 `now` 的窗不得再当 best window；顶层不得仍为已过 GO。
- `ovation_age_46m` / `ovation_age_91m`：分别满足 §4.1。

### 7.4 观测入口（API）

`GET /api/snapshots/latest` 必须：

**响应头（稳定，供 curl 验收）：**

- `X-Robots-Tag: noindex, nofollow`
- `X-Snapshot-Generated-At: <bundle.generated_at ISO>`
- `X-Snapshot-Source: live|lkg|bundled`（锁死这三个字面值的闭集；`loadLatestWithMeta().source` 同此闭集；禁止同义词、禁止等价枚举）
- `X-Aurora-Fallback-Used: true|false`
- `X-Ovation-Fetched-At`、`X-Kp-Fetched-At`、`X-Cloud-Fetched-At`（ISO；该源无效则空且同时 `X-Ovation-Health` 等为 `invalid`）
- `X-Ovation-Health` / `X-Kp-Health` / `X-Cloud-Health`：`ok|degraded|invalid`

**JSON（同源，供深查）：** 每源对象含 `source_time`（Kp 可 `null`）、`fetched_at`、`age_seconds`、`health`、`fallback_used`、`fingerprint`。`generated_at` 与头 `X-Snapshot-Generated-At` 字节级相同。

一期的 `X-Snapshot-Source: remote|bundled` 被替换；验收脚本改认新闭集，不要求兼容旧值。

### 7.5 核心验收：deploy 与 data 解耦

**在不产生 `snapshots/` commit、不产生新 Vercel deployment 的前提下，`/api/snapshots/latest` 的 `generated_at` 在 60 分钟内持续前进。**

操作（preview 先做，再生产）：

1. 记录当前 deployment 创建时间 `T_deploy`，以及 `T0 =` 第一次 `GET /api/snapshots/latest` 的 `X-Snapshot-Generated-At`。
2. **生产 §7.5 窗口开始前，必须临时 disable 一期 workflow 的 schedule**（GitHub UI 或注释 cron 行；这是暂停，不是退役：不删 workflow 文件、不改回滚面、测完立即恢复）。窗口 60 分钟内：Git `main` 无新的 snapshot commit；Vercel 无新 production deployment（deployment id 不变）。**Preview 的 §7.5 只要求该 preview deployment id 不变**，不要求 `main` 无 commit。
3. 每 10 分钟请求两次（第二次用于越过 ISR/Data Cache stale）。取第二次的头。
4. **单侧：**
   - 每个样本 `i`：`generated_at(i)` **不早于** `generated_at(i-1)`。
   - 最后一个样本：`generated_at` **不早于** `T0 + 40 分钟`（60 分钟窗口、10 分钟 refresh，允许丢一次再生，仍必须至少前进 40 分钟）。
   - 最后一个样本：墙钟 `now` **不早于** `generated_at` 超过 12 分钟（10 分钟 refresh + 2 分钟余量）。禁止 `generated_at` 比墙钟还新超过 1 分钟（时钟回拨/写错 now）。
   - 全过程 `X-Snapshot-Generated-At` **不早于** `T_deploy`（本条防止又在读烘进旧 deployment 的 JSON）。

本条失败 = 二期主目标失败，不得退役 workflow。窗口内因 schedule 未暂停而出现 commit/deployment 的，该窗口作废重测，不算通过也不算失败。

### 7.6 缓存与同源

- 跨过 10 分钟后连续两次 `GET /` 与两次 `GET /api/snapshots/latest`：不得把第一次 stale HIT 当失败；第二次的 `generated_at` 必须满足 §7.5 的 12 分钟单侧。
- 同一次 `next start` 进程内，首页 HTML 代表行（Fairbanks）的 status / best window，与 `/forecast/fairbanks` HTML、与 API `locations[]` 中 `location_slug=fairbanks` 的 `status` / `best_window_*` 一致。

### 7.7 故障注入（功能，墙钟不必 90 分钟）

用环境变量把上游打到不可达（或 fixture 服务器返回 5xx / 畸形 JSON），Blob 先预置一份合法 LKG：

- 立即请求：HTTP 200，**不是**全站 UNKNOWN；API `X-Aurora-Fallback-Used: true`；地点 status 来自 LKG+当前 now 重算。
- 畸形 OVATION：不得把畸形写入 Blob；随后健康请求仍能读到注入前 LKG（fingerprint 不变）。
- 缺云：能到的地点不得 GO；明确 `none` 的地点仍可 NO。
- Blob 不可用 + 上游不可用：落到 bundled；仍受硬限；不得 500 把全站打穿。

### 7.8 断源故障演练（>90 分钟，preview 必做）

preview 上把 OVATION/Kp/Open-Meteo 全部指向黑洞，LKG 里是一份 **GO** 地点（至少 Alaska 或 Fairbanks）：

| 墙钟 | 通过条件 |
|---|---|
| 0–10 分钟 | 全站不得因运输失败立刻 UNKNOWN。该 GO 地点在硬限内可以仍是重算后的 GO/MAYBE/NO，但 API `fallback_used=true`，Updated/source age 反映 LKG 真实年龄，不得显示成「刚刚 live 刷新」。 |
| 超过 OVATION 90 分钟 | 近窗不得再被该 LKG OVATION 驱动为无标识 GO。该地点按门控变为 UNKNOWN（无 Kp 可用时）或远窗 Kp 结果；**卡片不得继续展示无 degraded/UNKNOWN 标识的旧 GO。** |
| 超过云硬限（数字见 §9；未定稿前用「hourly 覆盖不再含今晚剩余窗」作为可测硬限） | 未 `none` 的地点封顶 MAYBE/low 或按引擎缺云规则；`none` 仍可 NO。 |

演练结束恢复上游后，下一次成功 fetch 的 `X-Aurora-Fallback-Used: false`，且 `X-Ovation-Fetched-At` **不早于** 恢复时刻超过 12 分钟。

### 7.9 合同与安全边界

- 仓库内运行时校验：任意 `cloud_block === "unknown"` 出现在 **engine 输出**中视为失败（类型曾经漂移的回归）。
- bundled / LKG 超过硬限后，curl 页面 HTML 不得在 `data-status="go"` 的首屏卡上呈现该过期 GO。
- `seo_indexable` 仍为 `false`；`curl -sI /` 与 `/api/snapshots/latest` 仍带 noindex。本条是防回归，不是上线收录。

---

## 8. 上线与回滚

### 8.1 灰度顺序

1. **实现 + 本地：** Python 拆分 → goldens → TS parity（§7.2 / 7.3）→ `next build && next start` 下 §7.1 / 7.4 / 7.7。
2. **Preview 部署**（Vercel preview，配 Blob token，**不**停 production 上的一期 workflow）：
   - 跑 §7.5（preview 60 分钟、无 snapshot commit、无新 preview deployment）。
   - 跑 §7.8（>90 分钟断源）。
   - curl 首页、Alaska、中纬州（Chicago 或 Wisconsin）、城市页（Fairbanks 或 Seattle）原始 HTML。
3. Preview 上述全部通过 → **production** 部署同一 git SHA。production 再跑一遍 §7.5（这是「解耦」的生产证据）。
4. **生产 §7.5 通过之前，不准退役 GitHub workflow**（删除 schedule/commit 步骤）。§7.5 窗口内按 §7.5.2 临时 disable schedule 是验收前置，不算退役；窗口结束立即恢复。bundled fallback 与一期 remote-read 仍是安全网。
5. 生产 §7.5 通过且至少再观察一个完整 90 分钟无全站误 UNKNOWN → 再退役 producer。

### 8.2 Workflow 退役时机

通过 §8.1.5 之后，同一 PR 或紧随 PR：

- 删除 schedule；删除 `git add snapshots` / `git commit` / `git push`。
- 可留 `workflow_dispatch` + `python3 engine/snapshot.py --offline` 或不写仓库的 health 打印，**默认不跑**。
- 关掉一期腾讯云 `workflow_dispatch` cron（ops，非代码）。它不是二期数据源。
- 不把 Vercel Cron 写进 `vercel.json`。

退役前 production 仍可出现 snapshot commit：那是一期安全网，不视为失败。唯一例外是 §7.5 的 60 分钟窗口——窗口已按 §7.5.2 暂停 schedule 却仍出现 commit/deployment，则该窗口作废重测（§7.5 末段）。

### 8.3 回滚

**回滚 = revert 二期合并提交 + 重启一期 workflow。**

- revert 后：`lib/snapshots.ts` 回到 raw.githubusercontent.com + bundled；`valid_until` 25 分钟语义恢复；API 头回到 `remote|bundled`。
- 立即恢复 `.github/workflows/refresh-aurora-snapshots.yml` 的 schedule（`7-57/10`）与 main 上的 commit/push。
- 恢复腾讯云 `workflow_dispatch` cron（与 GitHub 错位 5 分钟，一期 §3.5）。
- Blob 中的 LKG **不**需要删；revert 后代码不再读取即无影响。
- 不需要回滚 Wave 1 档案或视觉。

回滚触发：生产 §7.5 失败；或错误 LKG 导致无标识旧 GO 越过 §5.4；或 Function 超时使 ISR 大面积 500。

---

## 9. 风险与开放问题

下列条目报告已提出或实施时必须二选一。**本简报不擅自拍板数字或产品条款。** 审计应明确保留 / 改判 / 补数。

1. **Open-Meteo 非商业条款。** 免费 API 明确仅非商业。现状约 39 点/轮，中央 10 分钟缓存后约 144 轮/天，batch 后 HTTP 次数下降，但仍可能接近或需要商业 plan。站点若有广告/付费，这是合规问题，不只是 10k/day 配额。二期不签约、不换供应商；上线商业化前必须单独决定。
2. **Vercel Hobby 商业条款与配额。** ISR / Function duration / Data Cache / Blob 读写都走实际 plan。15 地点规模小，但 Hobby duration 可能撑不住未 batch 的上游；必须 batch。是否因商业使用违反 Hobby 条款：另议，不在本 patch 升级套餐。
3. **KV vs Blob。** 报告写「Blob/KV 一类」。本简报按阶段一结构与「唯一允许新依赖 `@vercel/blob`」走 Blob，理由候选（待审计确认，不当作已核实的计费事实）：需要的是整份 envelopes 的文档存储而非 Redis 语义；避免再引入 KV/Redis 包。若审计要求 KV，或 Hobby 实际没有 Blob 写权限，**停下来改简报，不得在实施中默默换存储。** 不在本简报同时设计两套持久层。
4. **报告并列的「单一 Python 运行时服务边界」。** §四把「迁 TS + goldens」与「Python 服务」并列。本简报走前者（报告所谓最稳实施的前半，且与「退役 Python producer」一致）。若审计要求保留 Python 为唯一 compute，则本简报的 §3 作废，不得在页面复制算法。
5. **Kp 软/硬年龄分钟数未定。** 报告禁止假设 10 分钟必变。定稿前 coverage（今晚 3h slots）是唯一硬闸。需要一段连续观测再写数字。
6. **Cloud 硬限分钟数未定。** 可测替代：hourly 时间轴不再覆盖今晚剩余 `night_slots` mids → 视为硬无效。若审计要墙钟硬限（例如 6h/24h），补进简报再写代码。
7. **degraded 显示真实 source age vs「不改文案」。** 只改 `formatUpdatedAt` 的时间输入，不改句子模板。若必须新增 “OVATION 91m ago” 这类独立字段，越出非目标，需另批。
8. **独立 watchdog。** 报告 P1：告警对象是 source refresh age / 全站 UNKNOWN 比率 / bundle age，且 **不能** 与主刷新共享唯一调度器。本简报非目标禁止新调度器，故 watchdog **不做**。§7.4 的 API 头可供人工或现有外监控拉取。未做自动 SLO 告警。
9. **Function 超时 vs Open-Meteo batch 形态。** 必须 batch/multi-coordinate；具体 URL 形态、一次几坐标、超时毫秒数按 plan duration 校准，简报不编 8s/45s 的新魔法数。约束：一轮 envelopes 刷新必须在单一 Function 时限内完成，失败走 LKG，不得把 ISR 拖死。
10. **`valid_until` 字段保留还是删除。** 二期不得再用它做门闩。删键可能让旧客户端/验收脚本崩。默认保留键、停止赋值 25min 语义；审计可改判删除。
11. **`answer_sentence` 时区格式。** Python 用 `strftime('%-I:%M %p')` + `tzname()`；TS 用 `Intl`。goldens 必须锁到字符串级；若平台 `%-I` 与 `America/Indiana/Indianapolis` 的 `tzname` 在 DST 边界与 JS 不一致，以 Python golden 为准并在 TS 复刻，禁止「看起来更本地化」。
12. **一期 raw.githubusercontent.com 路径。** 切流后主路径不再读 GitHub raw。生产未退役 workflow 前，bundled 仍会被 commit 刷新。§7.5 窗口必须显式冻结 commit，否则会误判。
13. **错误 LKG 污染。** 只写通过 schema 的 envelopes。仍可能写入「校验合法但科学错误」的上游。本简报不做 NOAA 内容合理性检查。硬限是唯一自动保险丝。
14. **`engine/.cache` 不是跨 run LKG。** 退役 Python 后这个问题消失。切流前不要把 Actions 本地 cache 当成生产降级路径。

---

---

## 执行备注（R1 轻微，不挡定稿，执行者知悉即可）

1. §2.5/§7.8 degraded 的 Updated 未锁喂 `source_time` 还是 LKG `fetched_at`；只要不喂 `generated_at` 即可，分钟数差异可接受。
2. 混源（一源 live、一源 LKG）时 bundle 级 `X-Snapshot-Source` 取值与 Blob 整份原子写 vs 按源合并未写死；实施时选一种并在代码注释写明。
3. §7.6 首页 HTML 无 `generated_at` ISO 外露，12 分钟单侧只对 API 头机械可判；同源 status/best window 比对不受影响。
4. §5.2「降 confidence」未锁高→中还是一律 low；落在 compute 之外，不影响 §7.2 金标。
5. §7.2 parity 未列 `answer_sentence`，§9.11 要求字符串级锁定；以 §9.11 为准，比对时补上该字段。

简报完成，等待审计。本轮不写代码。

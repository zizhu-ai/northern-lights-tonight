# 上线｜执行方案

> **已被取代：** 本文件仅保留为历史背景。当前上线加固的唯一批准规范是 [`docs/superpowers/specs/2026-08-24-launch-hardening.md`](docs/superpowers/specs/2026-08-24-launch-hardening.md)；冲突时以该规范为准。

**给谁：** 实现（Codex / 本地）  
**日期：** 2026-08-24  
**仓库：** https://github.com/zizhu-ai/northern-lights-tonight  
**生产自定义域：** `https://aurora-tonight.com`（`lib/site.ts` `SITE_URL`，已接 Vercel）  
**Vercel 默认域：** `https://northern-lights-tonight.vercel.app`（仍 200，与自定义域同一套站）  
**本文件地位：** 开 Google 收录前的上线合同。只覆盖本轮要做的事。  
**审计状态：** 定稿。2026-08-24 第 2 轮 Codex + Kimi 均为 0 严重 0 中等，停止审计，不得再开一轮。轻微见文末执行备注。

冲突时以上到下为准：

1. 本文件（含红线、阶段顺序、URL 白名单、冻结新文案）
2. `需求｜v1-Codex实现.md`（**例外：** 该文件「本轮仍 noindex / 不要提交 sitemap」由本文件 Phase C 废止）
3. `页面｜首版结构.md` 的 sitemap 白名单精神（以本文件 §4 表为准）
4. `content/ui-copy.json` 已有键（本轮只允许按 §7 **追加**，禁止改已有字符串）

---

## 0. 一句话

把已经能用的美国极光工具站，从「自定义域上的 noindex 预发」变成「只让 `aurora-tonight.com` 被 Google 收录」，并补齐隐私政策、联系邮箱、GA4、GSC。广告、Wave 2、地图、登录不做。

---

## 1. 现状（2026-08-24 已核实，实现时不得当未知）

| 事实 | 证据 |
|---|---|
| 产品页已在 `aurora-tonight.com` 跑 | `curl -sI https://aurora-tonight.com/` → 200，Vercel |
| `www` → apex、`http` → `https` 已 308 | `https://www.aurora-tonight.com/` Location 到 apex |
| `northern-lights-tonight.vercel.app` 仍 200，未跳主域 | `curl -sI` 无 Location |
| 全站禁止抓取 | `https://aurora-tonight.com/robots.txt` = `User-Agent: *` + `Disallow: /` |
| 页面 `noindex` | 首页 `robots: noindex, nofollow`；钱页 `noindex, follow` |
| 无 sitemap | `/sitemap.xml` → 404 |
| 无隐私/条款页 | `/privacy`、`/contact`、`/terms` → 404 |
| 页脚无邮箱 | 无 `mailto:` |
| 无 GA4 | 首页 HTML 无 `gtag` / `G-` |
| GSC 未做 DNS 验证 | apex TXT 仅有 Namecheap SPF，无 `google-site-verification` |
| 域名已有邮箱转发 | MX `eforward*.registrar-servers.com` + SPF `include:spf.efwd.registrar-servers.com` |
| 指南/methodology 的 description 泄漏草稿 | 线上 meta = `Pipeline stub — not indexed yet.`（来自 `app/layout.tsx`） |
| 首页/指南/near-me/methodology **无** canonical | 钱页有，`https://aurora-tonight.com/forecast/{slug}` |
| 无 `og:image` | 钱页 Twitter 卡为 `summary` |
| 上游 UA 写了竞品域名 | `lib/aurora-sources.ts`：`(+https://northernlightstonight.com; …)` |
| 引擎 `seo_indexable` 恒 `false` | `lib/aurora-engine/index.ts` 写死；页面 robots **并不读**该字段 |
| 一期 snapshot GitHub Action 已关 | workflow `disabled_manually` |
| `/forecast/boston` 已是 HTTP 404 | 不要改 |
| 二期请求驱动管线已 live | `/api/snapshots/latest` 有 `x-snapshot-source: live` |
| near-me 表单是客户端 `router.push` 到 forecast/view | 产品不生成 `/near-me?*`；本轮把它当成同一静态页，靠 canonical 收口，不为 query 单独 noindex |

---

## 2. 红线（按文违反 = 本轮失败）

1. **先挡住 vercel.app，再开收录。** Phase C 合并/部署前，生产必须满足：`curl -sI https://northern-lights-tonight.vercel.app/` 的 HTTP 状态为 301 或 308，且 `Location` 主机为 `aurora-tonight.com`（允许带 path）。不满足则禁止改 `robots.ts` 为允许抓取。
2. **只收录 §4 白名单。** `/view`、`/forecast/boston`、非 Wave 1 的 `/forecast/{slug}`、`/api/*`、任意 404 **不得**进 sitemap，不得 `index: true`。
3. **禁止用 `snapshot.seo_indexable` 或数据是否 stale 开关 robots。** 索引是发布期 URL 白名单。UNKNOWN 夜不得把钱页 noindex。本轮 **不要改** 引擎里的 `seo_indexable: false`。
4. **禁止广告、AdSense、`ads.txt`、百分概率、登录、地图、`/map`。**
5. **禁止重开** `.github/workflows/refresh-aurora-snapshots.yml`。
6. **禁止改** `content/ui-copy.json` 已有字符串；只允许 §7 列出的新键。
7. **禁止改** `content/guides/*.md` 正文。
8. **禁止** 把 User-Agent 或 canonical 写成 `northernlightstonight.com`、`*.vercel.app` 或其它非 `SITE_URL` 主机。
9. **禁止** 同一部署里「开了 gtag 却没有 `/privacy` 200」。
10. **禁止用 IP 或未点击的 GPS 个性化首页及其它可索引页** 的 SSR / H1 / OG / JSON-LD / 结论卡。`/view` 继续按现合同用 `lat`/`lng`/`name` 查询参数做 **noindex** 当次渲染（可出现在该页 kicker / H1 / OG）；本轮 **禁止改** `app/view/page.tsx` 的数据与路由实现。坐标 **不写入** 自有用户库、不用于再营销；不要求、也不声称「坐标从不出现在对自有源站的 HTTP 请求里」。
11. Preview 部署（`VERCEL_ENV !== "production"`）不得被这条 308 规则跳到生产域。

---

## 3. 阶段顺序（硬，不可对调）

两个生产部署，中间夹纯 ops。不得把 Phase C 和 Phase A 捏成一次部署碰运气。

```text
Deploy 1 = Phase A（代码，全站仍 noindex）
    ↓
Phase B（ops：邮箱别名、GSC DNS、GA4 属性、确认 vercel.app 已 308）
    ↓  §8 闸门全绿
Deploy 2 = Phase C（开收录 + sitemap）
```

Phase B 不写产品代码。Deploy 1 必须已含 vercel.app 的 308，这样 B 才能验收闸门。

---

## 4. 收录白名单（Phase C 生效；之前全部 noindex）

从 `WAVE_ONE_SLUGS`（`lib/forecast-places.ts`，现为 `地点档案/wave1.json` 的 locations 顺序）生成 forecast URL。禁止另维护一份 slug 表。

| URL | Phase C robots | sitemap |
|---|---|---|
| `/` | index, follow | 是 |
| `/near-me`（有无 query 同一份静态 HTML） | index, follow | 是（只列无 query URL） |
| `/forecast/{WAVE_ONE_SLUGS}` | index, follow | 是 |
| `/guides/best-time-to-see-northern-lights` | index, follow | 是 |
| `/guides/how-to-see-northern-lights` | index, follow | 是 |
| `/guides/where-to-see-northern-lights` | index, follow | 是 |
| `/methodology` | index, follow | 是 |
| `/privacy` | index, follow | 是 |
| `/terms` | index, follow | 是 |
| `/view` 及 `/view?*` | noindex, follow | 否 |
| `/forecast/boston` | 保持 HTTP 404 + 现有 noindex | 否 |
| 其它 `/forecast/{slug}` | 保持 HTTP 404 + noindex | 否 |
| `/api/*` | 保持 `X-Robots-Tag: noindex, nofollow` | 否 |
| 通用 404 | noindex, nofollow | 否 |

`robots.txt`（Phase C）：

```text
User-Agent: *
Allow: /
Disallow: /api/

Sitemap: https://aurora-tonight.com/sitemap.xml
```

允许爬 `/view`，靠 meta `noindex,follow` 防索引。禁止把 `/view` 写进 `Disallow`（否则谈不上 follow）。

---

## 5. Phase A — Deploy 1（仍全站 noindex）

本阶段 **不得** 改 `app/robots.ts` 的 `disallow: "/"`，**不得** 把任何页改成 `index: true`，**不得** 新增会被生产爬到的 sitemap（可先写 `app/sitemap.ts` 但若文件存在，Next 会挂 `/sitemap.xml`。**因此 Phase A 禁止创建 `app/sitemap.ts`。** sitemap 只在 Phase C 加）。

### 5.1 挡 vercel.app

新增 Edge `middleware.ts`（不要用会伤 preview 的全 `*.vercel.app` 一把抓）：

- 仅当 `process.env.VERCEL_ENV === "production"`
- 且请求 host（去端口、小写）**不是** `aurora-tonight.com` 且 **不是** `www.aurora-tonight.com`
- 则 308 到 `https://aurora-tonight.com` + `pathname` + `search`（含 `?`）
- localhost / `VERCEL_ENV=preview` / 未设 `VERCEL_ENV` 的本地 `next dev`：不跳
- 不改 path 大小写、不剥 query、不强制尾斜杠

`www` 已在平台 308 到 apex；中间件对 www 再 308 到 apex 可接受（仍停在 `aurora-tonight.com`）。

### 5.2 User-Agent

`lib/aurora-sources.ts` 的 `USER_AGENT` **精确改为：**

```text
NorthernLightsTonight/1.0 (+https://aurora-tonight.com; aurora forecast)
```

禁止从 `SITE_URL` 拼出其它 host。版本号本轮固定 `1.0`。

### 5.3 去掉 Pipeline stub

`app/layout.tsx`：

- 删除 `TODO(launch)` 注释
- `description` 改为 §7 `seo.fallback_description`
- **删除** layout 上的 `robots` 字段（避免根布局继续强制 noindex；各页 Phase A 仍自己写 `index: false`）
- 不在 layout 设 `index: true`

凡是现在没写 `description` 的可索引目标页，Phase A 就要写上 §7 的 description（即便仍 noindex），避免再继承 stub。

必须设 description 的页：

- `/` — 已有 `copy.home.lead`，保持
- `/near-me` — 已有 `copy.near_me.lead`，保持
- 三篇指南、`/methodology`、`/privacy`、`/terms` — 用 §7
- `/forecast/[slug]` — 保持现有动态 description
- `/view` — 保持现有

### 5.4 canonical

Phase A 就写上，指向 `SITE_URL`（即使仍 noindex）：

- 所有 §4「sitemap = 是」的 URL：`alternates.canonical` = `SITE_URL + path`（无尾斜杠；与现有钱页一致）
- `/near-me`：canonical **恒为** `https://aurora-tonight.com/near-me`（有 query 的请求也指向无 query URL）
- `/view`：**不设** canonical（包括不指向最近州页）

首页现在缺 canonical，必须补。

### 5.5 OG 图

添加 Next App Router 默认图：`app/opengraph-image.png`

- 1200×630 PNG
- 暮色带视觉（浅到夜），**禁止**库存极光照片、浏览器截图、竞品图
- 可有字标 “Northern Lights Tonight”，不要排长句 Title
- 不按 GO/MAYBE/NO 分图
- 验收：`/` 与任意钱页 HTML 出现 `og:image`，URL 主机为 `aurora-tonight.com`

Twitter：有图后允许变为 `summary_large_image`（Next 默认即可，不要手写拆套）。

### 5.6 页脚

`components/site-chrome.tsx` 的 `SiteFooter`，在现有 nav 链后面追加（顺序锁死）：

Tonight · Near me · Guides · How we decide · Privacy · Terms

其下两行（新键，§7）：

1. 可见 `mailto:hello@aurora-tonight.com`（锚文本就是该地址）
2. `copy.chrome.footer_sources`
3. 现有 `copy.chrome.footer_noaa` 保持最后一行，不改词

Privacy → `/privacy`；Terms → `/terms`。

### 5.7 `/privacy`

新页 `app/privacy/page.tsx`。英文。`lang` 继承 `en-US`。

Title：`Privacy Policy`  
H1：`Privacy Policy`  
description：§7 `seo.privacy`

正文必须出现下列 **H2 标题（逐字）** 和 **事实（可换句，不可换事实）**：

| H2 | 必须出现的事实 |
|---|---|
| Who we are | 站点名 Northern Lights Tonight；站点 URL `https://aurora-tonight.com` |
| Location data | 仅在用户点击 Use my location 后请求 GPS；浏览器端用坐标匹配 `us-places.json` 后 `router.push` 到 `/forecast/{slug}` 或 `/view?lat=&lng=&name=`；**不写入自有用户库、不用 cookie 记住位置、不用于再营销**。`/view` 的 URL 可以含坐标，服务端仅用于当次 noindex 渲染（现实现，本轮不改）。Vercel/CDN 访问日志若含 query，不在本轮承诺抹掉。 |
| Analytics | 若启用 Google Analytics 4：Google 可能用 cookie / IP；用于流量统计；无广告配置时不把 GA 当广告投放 |
| Data sources | NOAA SWPC（OVATION、Kp）；Open-Meteo 云量；浏览器不直打 NOAA |
| Contact | 邮箱 `hello@aurora-tonight.com` |

禁止：编造公司注册地址、欧盟代表、GDPR 同意横幅、出售个人数据的说辞（本站无账号、不卖名单）。  
本轮不做 cookie 横幅 / CMP。

Phase A robots：`index: false, follow: false`（与全站一致）。Phase C 再改成 index, follow。

### 5.8 `/terms`

新页 `app/terms/page.tsx`。

Title：`Terms of Use`  
H1：`Terms of Use`  
description：§7 `seo.terms`

H2 逐字：

| H2 | 必须出现的事实 |
|---|---|
| The forecast is not a guarantee | GO/MAYBE/NO 是工具判断，不是出门或安全建议 |
| NOAA | Not affiliated with NOAA；数据来自公开 SWPC 产品 |
| Your use | 夜间出行、驾驶、户外风险自负 |
| Contact | `hello@aurora-tonight.com` |

Phase A 同样 noindex。

### 5.9 GA4 代码（无 ID 则零输出）

- 环境变量名：**`GA_MEASUREMENT_ID`**（不要 `NEXT_PUBLIC_*`，避免文档里两种拼法）
- 只在 `app/layout.tsx` **服务端**读取；值为空或未设时，HTML **不得**出现 `googletagmanager.com` / `gtag(`
- 有值时：`afterInteractive` 注入官方 gtag 片段，测量 ID 必须与 env 字节级相同
- 不改结论卡、不阻塞 LCP（script 不得 `beforeInteractive`）
- 不接广告功能、不加 Google Ads 标签
- Phase A 部署时 **可以** 还不设该 env（则零输出）。Phase B 再设。若 Phase A 构建时已有该 env，则 **同构建必须已有 `/privacy` 200**（红线 9）

### 5.10 其它 Phase A 明确不做

- 不改引擎、快照、门控、Wave 1 档案
- 不改指南 markdown
- 不加 `app/sitemap.ts`
- 不改 `app/robots.ts`
- 不加 `error.tsx`、不加 Bing、不加 watchdog、不加 JSON-LD Organization
- 不把 `seo_indexable` 改 true

---

## 6. Phase B — ops（无代码）

在 Deploy 1 上线之后做。每一条都有机械验收。

1. **邮箱别名：** Namecheap Email Forwarding：`hello@aurora-tonight.com` → 操作者常用收件箱。验收：从非该域邮箱发一封到该地址，收件箱 10 分钟内可见。
2. **确认 5.1：** `curl -sI https://northern-lights-tonight.vercel.app/` → 301/308，Location 主机 `aurora-tonight.com`。再抽 `.../forecast/colorado` 应跳到 `https://aurora-tonight.com/forecast/colorado`。
3. **GSC：** 建 **域名属性** `aurora-tonight.com`（不要 URL-prefix 只绑 www）。apex TXT `google-site-verification=...`。不要把 `*.vercel.app` 加成属性。验收：GSC 显示已验证。此时仍 noindex，属正常。
4. **GA4：** 建 GA4 属性 + Web 数据流，网址 `https://aurora-tonight.com`。把测量 ID（`G-` 开头）写入 Vercel Production env `GA_MEASUREMENT_ID`。**不** 写入 Preview 也可以（Preview 保持无 gtag 可接受）。因为 layout 可能被静态预渲染：设 env 后需要 **一次 Production 重新部署** 才会出 gtag。这次重部署仍走 Phase A 代码（仍 noindex），**不算** Phase C。
5. 若第 4 步做了重部署：`curl -s https://aurora-tonight.com/privacy` 仍 200，且首页 HTML 出现该 `G-` ID；`robots.txt` 仍 `Disallow: /`。

---

## 7. 冻结新文案（只追加，不改旧键）

写入 `content/ui-copy.json`：

```json
"chrome": {
  "footer_privacy": "Privacy",
  "footer_terms": "Terms",
  "footer_sources": "Aurora data from NOAA SWPC. Clouds from Open-Meteo."
},
"seo": {
  "fallback_description": "Local aurora go / maybe / no for the US.",
  "best_time": "When and where the northern lights are most likely, by season, time of night, and latitude.",
  "how_to": "How to actually see the northern lights: darkness, looking north, waiting, and reading GO / MAYBE / NO.",
  "where": "Where to see the northern lights in the US tonight, plus standing regions versus event-only states.",
  "methodology": "How Northern Lights Tonight decides GO, MAYBE, NO, or UNKNOWN from NOAA and cloud data.",
  "privacy": "How Northern Lights Tonight handles location, analytics, and third-party data sources.",
  "terms": "Terms of use for Northern Lights Tonight, including forecast limits and NOAA disclaimer."
}
```

`chrome.footer_privacy` / `footer_terms` / `footer_sources` 是追加到现有 `chrome` 对象，不是覆盖整个 chrome。  
邮箱不要进 json（避免两处漂移）；页脚与隐私/条款正文写死 `hello@aurora-tonight.com`。

指南 Title/H1 仍来自 markdown frontmatter，不要用 seo.* 当 Title。

---

## 8. Phase C 闸门（全绿才能 Deploy 2）

用生产 URL 验收，全部必须为真：

1. `curl -sI https://northern-lights-tonight.vercel.app/` → 301 或 308，Location 主机 = `aurora-tonight.com`
2. `curl -s https://aurora-tonight.com/robots.txt` 仍含 `Disallow: /`（证明还未提前开收录）
3. `curl -s https://aurora-tonight.com/privacy` 含 `hello@aurora-tonight.com` 且含 H2 `Location data` 与 `Analytics`
4. `curl -s https://aurora-tonight.com/terms` 含 `Not affiliated with NOAA` 或正文明确 not affiliated（H2 `NOAA` 必须在）
5. `curl -s https://aurora-tonight.com/guides/best-time-to-see-northern-lights` 的 meta description **不含** `Pipeline stub`
6. `curl -s https://aurora-tonight.com/` 含 `rel="canonical"` 且 href 为 `https://aurora-tonight.com` 或 `https://aurora-tonight.com/`
7. 同上首页 HTML 含 `og:image`
8. 页脚含 Privacy、Terms、`hello@aurora-tonight.com`
9. `grep` 仓库：`northernlightstonight.com` **零命中**（`审计/` 与历史 markdown 除外；代码与 `content/` 零命中）
10. GSC 域名属性已验证（人工勾）
11. 若已设 `GA_MEASUREMENT_ID`：首页含该 ID，且 `/privacy` 仍 200；若未设：首页不含 `gtag(`（可先开收录，但本条必须二选一写进发布记录）

---

## 9. Phase C — Deploy 2（开收录）

只许动这些：

1. `app/robots.ts`：改为 §4 的 Allow/Disallow/Sitemap。`sitemap` 字段 = `https://aurora-tonight.com/sitemap.xml`
2. 新增 `app/sitemap.ts`：只输出 §4 表中 sitemap=是 的绝对 URL。`WAVE_ONE_SLUGS.map(slug => SITE_URL + "/forecast/" + slug)`。不要读 snapshots、不要读 `seo_indexable`。
3. 白名单页 `generateMetadata` / `metadata.robots` → `{ index: true, follow: true }`
4. `/view` 保持 `{ index: false, follow: true }`
5. `/near-me`：**保持** `export const dynamic = "force-static"`。不要为 query 读 `searchParams`、不要 `force-dynamic`。Phase C 本页 `index: true, follow: true`；canonical 仍恒为无 query 的 `/near-me`。产品表单不会生成 `/near-me?*`；若有人手工加 query，仍是同一静态页 + 同一 canonical。
6. 404 / boston：保持 noindex
7. `/api/snapshots/latest`：保持 `X-Robots-Tag: noindex, nofollow`
8. layout 继续不设 robots

Deploy 2 之后立刻：

- GSC 提交 `https://aurora-tonight.com/sitemap.xml`
- URL Inspection：`/`、`/forecast/colorado`、`/forecast/alaska`、`/forecast/fairbanks`、`/guides/best-time-to-see-northern-lights`、`/view`（期望：前五可索引，`/view` 被 noindex）
- 确认 Alaska 与 Fairbanks 的 Title 仍不同（现有逻辑，本轮禁止改 Title 公式）

---

## 10. Phase C 后机械验收（Deploy 2）

```bash
# robots
curl -s https://aurora-tonight.com/robots.txt
# 必须含 Allow: / ；必须含 Disallow: /api/ ；必须含 Sitemap: https://aurora-tonight.com/sitemap.xml
# 不得含 Disallow: /view

curl -s https://aurora-tonight.com/sitemap.xml
# 必须含 SITE_URL 的 / 、 /near-me 、 /privacy 、 /terms 、 三篇 guides 、 /methodology
# 必须含 15 个 /forecast/{slug}（与 WAVE_ONE_SLUGS 同集）
# 不得含 /view 、 /api 、 /forecast/boston

curl -sI https://northern-lights-tonight.vercel.app/ | grep -i location
# 主机 aurora-tonight.com

curl -s https://aurora-tonight.com/ | grep -i 'name="robots"'
# index, follow（允许无空格变体 index,follow）

curl -s https://aurora-tonight.com/view | grep -i 'name="robots"'
# noindex

curl -sI https://aurora-tonight.com/forecast/boston
# 404

curl -s https://aurora-tonight.com/guides/best-time-to-see-northern-lights | grep -i 'meta name="description"'
# 不得含 Pipeline stub
```

首页 SSR 仍必须含 15 行地点表；不得出现「你在某城今晚 MAYBE」。

---

## 11. 本轮明确不做

- 广告 / AdSense / ads.txt / 联盟链接
- Open-Meteo 商业合同（无广告则本轮不签；**接广告前必须另开 spec**）
- Vercel 套餐升级（另议；本文件不改 `vercel.json` 除中间件所需之外——本轮 **不要改** `vercel.json`，中间件用 `middleware.ts`）
- Wave 2 地点、`seo_indexable: true`、把 JSON-LD 补成 Organization
- cookie 同意横幅、独立 `/contact` 页、Bing、IndexNow、`llms.txt`
- 自定义 500 页、数据 watchdog（可事后用 Uptime 打 `/api/snapshots/latest`，不在本仓库做）
- 改判断引擎、TTL、Blob、GitHub Action
- 为 GA4 实现 PRD 里整串自定义事件（有 `page_view` 即可；gtag 默认 page_view 足够）

---

## 12. 文件清单（只许这些，除非为 OG 图多一个 png）

**Deploy 1：**

| 文件 | 动作 |
|---|---|
| `middleware.ts` | 新增，§5.1 |
| `lib/aurora-sources.ts` | 只改 `USER_AGENT` |
| `app/layout.tsx` | stub / robots 字段 / 可选 GA 注入 |
| `app/page.tsx` | canonical |
| `app/near-me/page.tsx` | canonical 恒 `/near-me`（仍 noindex；保持 `force-static`） |
| `app/guides/*/page.tsx` | description + canonical（仍 noindex） |
| `app/methodology/page.tsx` | 同上 |
| `app/privacy/page.tsx` | 新增 |
| `app/terms/page.tsx` | 新增 |
| `app/opengraph-image.png` | 新增 |
| `components/site-chrome.tsx` | 页脚链 |
| `content/ui-copy.json` | 只追加 §7 |

**Deploy 2：**

| 文件 | 动作 |
|---|---|
| `app/robots.ts` | §4 / §9 |
| `app/sitemap.ts` | 新增 |
| 白名单各页 `metadata.robots` | 改为 index,true |
| `app/near-me/page.tsx` | 只改 `robots` 为 index,follow；**不要**加 searchParams 分支、**不要**去掉 `force-static` |
| `app/view/page.tsx` | **禁止改实现**；只确认 layout 不再覆盖其 `noindex,follow` |

禁止改：`engine/`、`地点档案/`、`content/guides/*.md`、`.github/workflows/`、`vercel.json`、`lib/aurora-engine/`、`lib/live-snapshots.ts`。

---

## 13. 验收清单（实现者自勾）

**Deploy 1**

- [ ] vercel.app 生产 308 到主域（含一个子路径）
- [ ] preview 部署打开不跳生产
- [ ] UA 已改；仓库代码无 `northernlightstonight.com`
- [ ] 任意指南页 description 无 `Pipeline stub`
- [ ] `/privacy` `/terms` 200，H2 与邮箱符合 §5.7–5.8
- [ ] 页脚顺序与 mailto
- [ ] 首页有 canonical 与 og:image
- [ ] `robots.txt` 仍 `Disallow: /`
- [ ] `/sitemap.xml` 仍 404
- [ ] 未设 GA env 时 HTML 无 gtag；有 env 时有 gtag 且 privacy 在

**Phase B**

- [ ] hello@ 能收信
- [ ] GSC 域名属性已验证
- [ ] （可选）GA4 env + 一次仍 noindex 的重部署

**Deploy 2**（闸门 §8 全绿之后）

- [ ] §10 全部 curl 通过
- [ ] sitemap 无 /view /boston /api
- [ ] GSC 已提交 sitemap
- [ ] Alaska / Fairbanks Title 不同

---

## 执行备注

来自 2026-08-24 第 1 轮审计（Codex 1S1M 已改正文；Kimi 0S0M 的轻微不改正文）。**不挡定稿、不要为这些再开一轮。**

1. 删 layout `robots` 后不再有继承兜底。Deploy 1 每个会输出的 HTML 路由都必须自己写 `index: false`（含新建 `/privacy` `/terms`；guides / methodology / near-me / view / 首页 / 钱页现状已自带）。
2. `og:image` 主机为 `aurora-tonight.com` 依赖现有 `metadataBase: new URL(SITE_URL)`；不要另造绝对域名。
3. Phase A 的 `/near-me` 即使带手工 query，也输出同一静态 HTML；Phase C 不靠 robots 区分 query。
4. §7 的 `seo` 对象是新增整块；`chrome` 三键是追加。不要覆盖整个 `chrome`。
5. 页脚六链顺序以 §5.6 锁死为准，即使实现时发现旧脚注顺序不同。

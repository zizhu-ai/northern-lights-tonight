# 设计简报增补｜数据管道一期：部署与数据解耦（2026-08-23）

**地位：** 工程实施简报。不改任何视觉与文案；视觉仍以《设计简报｜视觉方向D-暮色带.md》+《设计简报增补｜清账-20260822.md》为准。
**依据：**《审计/codex-pipeline-options-20260823.md》（codex sol medium 独立架构复核）。

## 1. 背景与事故

- 现状链路：GitHub Actions cron `*/10` 跑 `engine/snapshot.py` → commit `snapshots/*.json` 到 main → 触发 Vercel 部署。页面 `force-static` + ISR 600s，读的是**部署那一刻打进 deployment 的快照**（`lib/snapshots.ts` 用 `readFile`）。
- 快照 `valid_until = generated_at + 25min`。过期后全站翻 UNKNOWN（"Source data is too old to treat as live."）。
- 2026-08-23 事故：GitHub 调度在 00:45–02:14 UTC 之间停顿约 90 分钟（workflow 状态 active、历史 run 全 success，纯调度延迟）。01:10 起全站 UNKNOWN 超过 1 小时。NOAA 本身正常。
- 结构性问题：**站点活性 = 每 25 分钟内必须完成「调度→运行→push→Vercel 构建→部署」全链**。任何一环抖动，核心功能全站失效。

## 2. 目标与非目标

### 目标

- **把 Vercel 部署从数据新鲜度链路里拿掉**：页面 ISR 重渲染时直接读 Git 仓库 main 分支上的最新快照（raw.githubusercontent.com），新 commit 不再需要等构建部署即可生效。
- workflow 小幅加固：防重叠、限时。
- 增加一个**独立的外部调度器**触发同一 workflow（workflow_dispatch），消除「GitHub schedule 单点延迟」这一事故源。
- UNKNOWN 语义不变：`valid_until` 过期仍 UNKNOWN（derived windows 依赖生成时刻的 `now`，一期不动这个语义，二期按源分级）。

### 非目标（二期做，本期不做）

- Python 引擎迁 TS、服务端用当前时间重算派生快照。
- 按数据源（OVATION/Kp/云量）分级的新鲜度判定；`valid_until` 25 分钟保持不变。
- Vercel Blob/KV 持久层、last-known-good 降级、source age 观测字段。
- Open-Meteo 批量请求优化、noindex 上线 gate。
- 不新增任何 npm 依赖。

### 一期边界声明（诚实声明）

一期之后，「GitHub Actions 整体宕机」或「两个调度器同时失灵 ≥25 分钟」仍会导致 UNKNOWN。一期消除的是：部署延迟/构建失败、单一 schedule 延迟（昨天的事故类型）。彻底的解法在二期。

## 3. 设计

### 3.1 数据流（改后）

```
GitHub Actions cron */10  ─┐
                           ├─→ snapshot.py → commit snapshots/ → main（现状不变）
腾讯云 cron */10 (错位5min)─┘   （dispatch 触发，见 §3.4）
                                         │
页面 ISR render（revalidate 600）          │
  └─ loadLatest() / loadForecastSnapshot() │
       ├─ ① fetch raw.githubusercontent.com/.../main/snapshots/<file>（next.revalidate: 120，5s 超时）
       ├─ ② readFile 本地 bundled 快照（并行）
       └─ 取 generated_at 较新且解析有效的一份；remote 失败/无效/404 → bundled
```

### 3.2 `lib/snapshots.ts` 改动（核心，唯一数据入口）

所有快照读取只经过 `loadLatest()` / `loadForecastSnapshot(slug)` 两个函数（调用方：`app/page.tsx`、`app/forecast/[slug]/page.tsx`、`components/tonight-places.tsx`、`app/api/snapshots/latest/route.ts`），**只改这两个函数的实现，签名与返回类型不变**。

- 新增模块级常量：remote base 默认 `https://raw.githubusercontent.com/zizhu-ai/northern-lights-tonight/main/snapshots`，可被环境变量 `SNAPSHOT_REMOTE_BASE` 覆盖（用于测试与故障注入）。
- `remote = fetch(`${base}/${file}`, { next: { revalidate: 120 }, signal: AbortSignal.timeout(5000) })` 与 `bundled = readFile(...)` 并行发起。
- 选取规则：
  1. remote 响应非 2xx、网络/超时错误、JSON 解析失败、缺 `generated_at` 或 `generated_at` 无法 `Date.parse` → 用 bundled。
  2. 两边都有效 → 用 `generated_at` 较新的一份（相等时用 remote，因为它更可能接近真实现状）。
  3. bundled 文件不存在（ENOENT）且 remote 不可用 → 维持现状行为：`loadLatest` 抛错、`loadForecastSnapshot` 返回 `null`。
  4. remote 404（如 boston）→ 落到规则 1，由 bundled 的 ENOENT 决定返回 `null`。
- `loadLatest` 额外校验 `Array.isArray(locations)`，不满足视为无效。
- `isSnapshotFresh` 及 UNKNOWN 判定逻辑**一字不改**。

### 3.3 `app/api/snapshots/latest/route.ts`

- 去掉 `force-static`，改为 `export const revalidate = 120`。
- 响应头在现有 `X-Robots-Tag` 基础上增加：`X-Snapshot-Source: remote|bundled`、`X-Snapshot-Generated-At: <iso>`（供观测与验收）。
- 为此 `loadLatest()` 需能把 source 信息传出来：新增导出函数 `loadLatestWithMeta(): Promise<{ data: SnapshotBundle; source: "remote" | "bundled" }>`，`loadLatest()` 改为调用它并只取 `.data`。页面调用方继续用 `loadLatest()`，零改动。

### 3.4 workflow 加固（`.github/workflows/refresh-aurora-snapshots.yml`）

- 加 `concurrency: { group: refresh-aurora-snapshots, cancel-in-progress: false }`。
- job 加 `timeout-minutes: 8`（现状一轮约 2 分钟，39 个串行云量请求是最坏路径）。
- cron 错开整点高峰：`*/10` 改为 `7-57/10 * * * *`（:07/:17/:27/:37/:47/:57），避开 GitHub 文档明示的整点高负载。
- 其余不变（继续 commit，bundled fallback 因此永远不至于太旧）。

### 3.5 外部调度器（ops 步骤，非代码）

在腾讯云 121.5.14.88 加一条 cron（与 GitHub cron 错位）：每 10 分钟 `curl -X POST` 调 `workflow_dispatch` 触发同一 workflow（用 repo fine-grained PAT，仅存服务器 root crontab，不进仓库、不进本报告）。

- 命令模板：`curl -sf -X POST -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" https://api.github.com/repos/zizhu-ai/northern-lights-tonight/actions/workflows/refresh-aurora-snapshots.yml/dispatches -d '{"ref":"main"}'`
- dispatch 在 GitHub schedule 延迟时仍会及时运行；两者互为冗余。GitHub Actions 整体宕机属于一期边界（见 §2）。

### 3.6 明确的取舍

- raw.githubusercontent.com 自带 CDN 缓存（实测约 5 分钟）。叠加 fetch revalidate 120s + ISR 600s，端到端数据年龄最坏 ≈ 17 分钟 + cron 间隔，对 25 分钟 `valid_until` 仍有正余量；可接受，一期不引入新存储。
- 不比较 `valid_until` 选源，只比 `generated_at`：新旧与有效性由统一的 `isSnapshotFresh` 在页面层判定，保持单一事实源。

## 4. 改动文件清单

1. `lib/snapshots.ts` — §3.2、§3.3 的 `loadLatestWithMeta`。
2. `app/api/snapshots/latest/route.ts` — §3.3。
3. `.github/workflows/refresh-aurora-snapshots.yml` — §3.4。
4. 服务器 crontab — §3.5（ops，无 PR 内容）。

其余文件零改动（页面、组件、文案、样式、类型均不动）。

## 5. 验收标准（逐条可测）

1. `npm run build` 通过；`next start` 后首页与 `/forecast/chicago` 正常渲染。
2. **故障注入**：`SNAPSHOT_REMOTE_BASE=https://127.0.0.1:1` 起服务，页面仍 200 渲染（走 bundled），无未捕获异常。
3. **fresher-wins**：本地把 `snapshots/latest.json` 的 `generated_at` 改成 2 天前后起服务，首页展示的 `Updated` 仍按 remote（较新）计算；改回后恢复。
4. **404 语义**：`/forecast/boston` 仍为 404 页。
5. **UNKNOWN 语义**：在与第 2 条相同的故障注入下（`SNAPSHOT_REMOTE_BASE` 不可达，页面走 bundled），把本地 bundled `snapshots/chicago.json` 的 `valid_until` 改到过去，验收 `/forecast/chicago` 卡片显示 UNKNOWN + `Source data is too old to treat as live.`。
6. **SSR**：`curl` 首页原始 HTML 含 `data-status=`。
7. **API**：`curl -i /api/snapshots/latest` 返回 200，含 `X-Snapshot-Source` 与 `X-Snapshot-Generated-At`。跨 120s 以上连续请求两次，至少一次满足单侧条件：raw main 上 `latest.json` 的 `generated_at` − `X-Snapshot-Generated-At` ≤ 7 分钟（余量 = raw CDN ≈5min + fetch revalidate 120s）。header 比 raw **更新**不限（fresher-wins 选中较新 bundled 属正确行为），只禁止比 raw 旧得超过余量。
8. **上线后核心验收**：记录当前 production deployment 的创建时间 `T_deploy`。当 GitHub main 出现 `generated_at` 晚于 `T_deploy` 的新快照 commit 后，`curl /api/snapshots/latest` 满足：`X-Snapshot-Source: remote`，且 `X-Snapshot-Generated-At` 晚于 `T_deploy`。若下一次快照 commit 前 Vercel 先落地了新 deployment（赛跑），则等再下一次快照 commit 重测。这一条直接证明「数据前进不再需要部署」。
9. workflow 下一次运行可见 `concurrency` 生效（run 页面显示 group），腾讯云 dispatch 触发一次成功 run（event = `workflow_dispatch`）。

## 6. 上线与回滚

- 分支：`fix/snapshot-remote-read` → PR → squash merge（merge 前向用户确认）。
- 部署后先验 §5.7/5.8，再配 §3.5 的服务器 cron。
- 回滚：revert 合并提交即回到纯 bundled 行为（本期无破坏性变更，snapshots/ 继续按现状提交）。

## 执行备注（R1 轻微，不挡定稿，执行者知悉即可）

1. §3.2 规则 4 把 boston 写成「remote 404」场景；实际 `/forecast/boston` 是独立静态页恒 `notFound()`，不经快照读取。§5.4 验收不受影响。
2. §5.3「2 天前后」表述含混；意图是把 bundled 改旧，使 remote 必然较新。
3. §5.2 页面上观测不到「走 bundled」的来源标记，该条只验收 200 渲染与无未捕获异常；来源标记以 §5.7 的 API 头观测。
4. §6 的顺序下，§5.8 窗口期内外部调度器（§3.5）尚未配置，该窗口仍单点依赖 GitHub schedule；属已知顺序取舍。
5. §3.5 crontab 分钟表达式未写死；实施时取 `2-52/10 * * * *`（与 GitHub 的 :07 起错位 5 分钟）。

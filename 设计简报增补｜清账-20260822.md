# 设计简报增补｜清账（2026-08-22）

**给谁：** grok（执行实现）
**性质：** 对已定稿《设计简报｜视觉方向D-暮色带.md》的补丁。主简报全部决策不变；本增补只清以下欠账。每条都已拍板，照做即可，不自由发挥。
**硬约束沿用：** 不动引擎/URL/判断逻辑；不加依赖；不碰 git；文案改动仅限本增补列明的条目；`npm run build` 必须过。

## 1. 钱页 stale 文案去重（P1）

`!live` 时 "Source data is too old to treat as live." 现在出现在：带内答案句、卡内 Main issue、Tonight's hours 兜底、Why this verdict 网格 5–6 行。改为：

- 带内答案句：**保留**（首屏要有一句人话）。
- 卡内 Main issue：**保留**。
- Tonight's hours：fallback 改为单行 "Hours depend on live data. We don't guess."（在 `ui-copy.json` 加文案，不内联）。
- Why this verdict 网格：`!live` 时整节替换为**一行**——"Data live → Source data is too old to treat as live."——不再逐行重复。

## 2. 焦点环浅底对比（P1）

`globals.css`：新增 `--focus-strong: #0D6B4A`（白底 5.7:1）。规则：`:focus-visible` 默认用 `--focus-strong`；仅暮色带内、夜卡上、页脚夜色区继续用 `--focus: #3CDBA0`。实现方式自定（如 `body:has(.twilight-band) .site-header :focus-visible` 这类限定，或对暗色容器单独覆盖），验收只看实测对比：浅底焦点环 ≥ 3:1，暗底不变。

## 3. Try again 安慰剂（P2）

`try-again-button` 的 `window.location.reload()` 对静态缓存页永远无效。改为：

- 按钮从卡上**移除**（四态都移除）。
- UNKNOWN/暂停态卡内底部改为一行静态文案（进 `ui-copy.json`）："We check sources every 10 minutes. This page updates itself."，后跟一个指向 `/methodology` 的文本链接 "How we decide"。
- `/view` 页的 Try again 同样移除，空态本来就有搜索框，不需要它。

## 4. Boston 软 404（P2，上线前必须）

`app/forecast/boston/page.tsx` 现在返回 HTTP 200 渲染"没有 Boston 专页"内容。改为**真 404 状态 + 保留吸收 UI**：

- 把现有吸收内容挪到 `app/forecast/boston/not-found.tsx`；
- `page.tsx` 改为直接调用 `notFound()`；
- 验证：`curl -s -o /dev/null -w '%{http_code}' /forecast/boston` 返回 404，且页面仍渲染吸收文案和指向 Massachusetts 的链接。

## 5. 钱页瘦版带（轻微）

`app/forecast/[slug]/page.module.css`：删掉桌面 `min-height: calc(42vh - var(--header-height))`（钱页带高由内容决定：kicker + H1 + 答案句 + padding）。首页带维持现状不动。

## 6. 正文行宽（轻微）

指南、methodology、钱页答案句实测 ~86ch。把阅读性正文的行宽收到 **≤ 68ch**（指南正文、methodology 正文、`.answer` 段落）。表格、卡片、带内 H1 不受此限。

## 7. "Look / North" 同义反复（轻微）

卡内 meta 的 `dt: Look / dd: North` 改为 `dt: Look toward / dd: North`（文案进 `ui-copy.json`；有 `look_toward` 数据时用数据值，没有用 dossier 默认值，保持现有数据来源逻辑不变）。

## 8. 钱页 kicker 去掉 Headline（轻微）

`app/forecast/[slug]/page.tsx` kicker 由 `{name} · {tz} · Headline: {point}` 改为 `{name} · {tz}`。代表点信息不再需要出现在这里。

## 9. 非带内页面的 H1 尺寸（轻微）

`.hero h1` 的 34/56 只应作用于暮色带内。指南 / methodology / near-me / 404 的 H1 收到 30/40（手机/桌面），字重不变。

## 10. 断点统一（轻微）

`components/tonight-places.module.css:98` 的 599px 卡化断点改为 768px，与全站一致。

## 11. Find place 面板阴影（轻微）

`globals.css:295` 附近：Find place 面板的 `0 12px 32px` 大阴影移除，改 1px `line` 色边框（面板靠边框不靠阴影）。判定卡的大阴影不动。

## 12. 桌面行 hover 不可见（轻微）

首页/列表行 hover 从 `#FFFFFF` on `#F4F6F8` 改为可见反馈：hover 底色用 `#E9EDF2`（对纸面约 1.15:1 可辨，或给 hover 行加 1px `line` 边）。二选一，可见即可。

## 13. 暂停态首页行空第三列（轻微）

全 UNKNOWN/暂停时，首页表行的 window 单元格为空字符串导致胶囊漂浮。空 window 时不渲染该单元格（或整列在暂停态隐藏），保持行对齐。

## 验收（逐条机械判定）

- build 过；`/forecast/boston` HTTP 404 且有吸收 UI。
- 钱页 `!live` 时 "Source data is too old to treat as live." 全页出现 ≤ 2 次（带内 1 + 卡内 1）。
- 卡上无 Try again 按钮；有自动更新文案 + methodology 链接。
- 浅底焦点环实测 ≥ 3:1；指南/methodology 正文 computed max-width ≤ 68ch。
- 钱页桌面带高 = 内容高（无 min-height 死带）。
- 9–13 各条 diff 可见。

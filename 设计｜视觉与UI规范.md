# 设计｜视觉与 UI 规范

**给谁：** 产品审视觉方向  
**日期：** 2026-08-21  
**不改：** URL、模块顺序、GO/MAYBE/NO/UNKNOWN、SSR 结论必须在 HTML  
**视觉稿：** `视觉稿/index.html`（静态，非生产）

---

## 1. 结论：做成「夜里的决定，白天的文章」

不要做成 SpaceWeatherLive 那种仪表盘，也不要做成旅游博客的极光大图站。

这个产品的瞬间是四个词：GO / MAYBE / NO / UNKNOWN。其它都是解释。视觉上只需要一件事成立：

**打开地点页，手机不用滚，人已经看见今晚值不值得出门。**

所以把页面劈成两层：

| 层 | 气质 | 用在哪 |
|---|---|---|
| 白天 | 浅灰纸、白卡片、衬线标题 | 顶栏、H1、搜索、15 行表、Why、FAQ、指南 |
| 夜里 | 海军底、大状态词、一点极光绿 | **只有结论卡**（以及页脚夜色收口） |

理由：

1. 搜 `tonight` 的人很多是下午在办公室，不是 11 点在野地。整页 OLED 黑在白天刺、长文也累。
2. Google 摘要/截图、常青 FAQ、指南，浅色更像可信文章，不像 App 壳。
3. 结论卡是产品。做成「窗户」：纸上开一扇夜窗，状态词是夜空里最大的字。
4. 现有 stub 已经是 `#0b1020` 全黑。那是管线页，不是要上的产品气质。

备选（不推荐 v1）：全站暗色。更「工具」，更像竞品仪表盘，长文和首页表都会变差。若要暗色，也必须先保证结论卡对比度，而不是先上粒子极光。

---

## 2. 五条硬规范

1. **状态词是视觉主角色。** H1 是问句（衬线、约 30px 手机），卡里 MAYBE 是 52–64px 无衬线。不要反过来做成巨大 H1 + 小徽章。
2. **颜色不单独表示状态。** 必须「词 + 色 + 形」（胶囊）。NO 用冷灰，不用报警红。红只留给 GPS 失败、500。
3. **禁止装饰性极光。** 无库存极光英雄图、无 WebGL 粒子、无 Kp 仪表、无百分环、无地图。LCP 和信任都输在这些上。
4. **结论是 HTML，皮肤是 CSS。** 关 CSS 仍能读到 MAYBE、窗口、主障碍。
5. **触控 44px，对比 AA。** 状态色在夜卡上的对比 ≥ 4.5:1；浅底上的正文也是。

---

## 3. 设计 token

### 3.1 颜色

| Token | Hex | 用途 |
|---|---|---|
| ink | `#122033` | 正文、顶栏字标、主按钮 |
| ink-soft | `#3D4D5C` | 导语 |
| muted | `#5C6B7A` | 区标题、辅助 |
| paper | `#F3F5F7` | 页底 |
| paper-2 | `#FFFFFF` | 卡片、顶栏 |
| line | `#D8DEE6` | 边框 |
| night | `#0C1522` | 结论卡、页脚 |
| night-2 | `#152033` | 卡顶渐变 |
| aurora | `#3CDBA0` | GO、焦点环、小标志 |
| maybe | `#E8B44C` | MAYBE |
| no | `#8A93A3` | NO |
| unknown | `#7EB0FF` | UNKNOWN |
| danger | `#E25B4C` | 真错误 |

浅底胶囊：

| 状态 | 字 | 底 |
|---|---|---|
| GO | `#0D6B4A` | `#D9F7EA` |
| MAYBE | `#7A5710` | `#FBEEC8` |
| NO | `#4D5663` | `#E7EBF0` |
| UNKNOWN | `#21548E` | `#E1EDFF` |

### 3.2 字体

- **UI / 状态词 / 表：** Inter（或系统 ui-sans）
- **H1 问句：** Newsreader（衬线）。这是「杂志夜报」而不是仪表盘。指南正文也可衬线；工具页（near-me、表）保持无衬线。
- 不要用太空/霓虹/手写字体。

### 3.3 字号（手机 → 桌面）

| 角色 | 手机 | 桌面 |
|---|---|---|
| Caption / 区标题 | 11–12 | 12 |
| UI | 14 | 14 |
| 正文 | 16 | 16 |
| 导语 | 18 | 18 |
| H1 | 30 | 40 |
| 状态词 | 52 | 64 |

行高：H1 1.18，正文 1.5，状态词 1.0。正文行宽 60–66ch。

### 3.4 间距与圆角

- 网格 8。页边 16 / 桌面 24。
- 卡片圆角 16，输入 12，胶囊 999。
- 顶栏 56。主按钮、输入、表行最小 44 高。
- 桌面钱页：主栏 ~680 + 右栏 240 + 间隙 40。首页内容宽 960。

### 3.5 阴影与动效

- 只有夜卡用大阴影：`0 12px 40px rgba(12,21,34,.28)`，MAYBE/GO 可加极弱色晕。
- 白卡片靠 1px 边，不靠阴影墙。
- 动效 ≤ 200ms。`prefers-reduced-motion: reduce` 全关。不要循环呼吸灯。

---

## 4. 组件

### 顶栏

白底、1px 底边、字标左、Find place 右。桌面中部 Tonight / Near me / Guides。无账号。标志是 22px 方圆角 + 两道极光弧，不要写实地球。

手机：只留字标 + Find place。链进页脚。

Find place：手机用底栏全宽（sheet），桌面从按钮下弹出约 420px。不要居中大模态挡住第一屏结论。

### 结论卡

上沿 3px 状态色条。大词 → 人话 → 四格 meta（Best window / Main issue / Look / Confidence）→ Updated + Share。

四态只换色和文案，**骨架不动**。UNKNOWN 的 Share 换成 Try again。过期快照走 UNKNOWN，禁止残留旧 GO 绿。

Share 是卡内次按钮（白 8% 底），不要做成巨大 CTA 盖过状态词。

### 小时轴

白底列表，不是图。时间等宽数字。SKIP 变灰。GO 行淡绿底。默认约 5 行，「Rest of the night」展开。必须在 SSR 里，可跨折页。

### 首页 15 行

桌面表；手机一行一张可点卡：胶囊 + 地名 + 窗口。整行是热区。不要首页地图。

### 指南

680px 栏。顶部一条浅「Check tonight → /」，不嵌实时引擎。H1 可用衬线；正文 16/28。

---

## 5. 逐页视觉要点

**首页：** 问句 + 两句「取决于地点」+ 搜索 + 表。不要 IP 结论卡。搜索是转化，表是 SEO 主体。

**钱页：** 第一屏 = kicker（州 + 时区）+ H1 + 答案段 + 夜卡。右栏桌面粘性 Nearby，手机挪到 FAQ 上。Oregon 卡代表点仍是 Baker City。

**near-me：** 工具感，浅底，H1 不要写成 Can You See Tonight。隐私一句用 muted，不要法律墙。

**view：** 同一夜卡。无快照时 UNKNOWN 是默认，不要装 live。壳与全站相同。

**404：** 浅底、一句人话、给吸收目标（Boston → Massachusetts），不要夜卡装结论。

---

## 6. 不要做

- 库存极光、星空粒子、玻璃拟态堆叠
- 百分数、Kp 仪表、热力地图
- NO 用红、GO 用闪烁
- 把广告插进 H1 和卡之间（以后若有广告，卡后）
- 为暗色再做一套互斥布局
- 用客户端把结论「画出来」才出现

---

## 7. 实现时怎么落地

- CSS 变量按第 3 节一次写进全局。不要每页一套。
- 状态用 `data-status="maybe"` 上卡，不要四套卡片组件。
- 字体：`next/font` 引 Inter + Newsreader，`html lang="en-US"`。
- 焦点环用 aurora 绿，键盘可走完 Find place。
- 先做手机钱页第一屏，再桌面，再指南。

视觉稿是给审方向的静态页，不是 v1 实现。实现仍以冻结线框顺序为准，只换皮肤。
